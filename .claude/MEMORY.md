# Project Memory

## Project Overview
- **Name**: VisionQuest
- **Description**: AI-coach portal for SPOKES workforce development; Sage (Gemini 3.1 Flash Lite) acts as a chat-first site manager
- **Tech stack**: Next.js 16, TypeScript, Prisma 6, Supabase Postgres (pgvector) + Storage, Tailwind 4, Playwright
- **Repo**: https://github.com/doclegg05/VisionQuest.git · Live: https://visionquest.onrender.com

## Current Status
**2026-09-05 Match & Connect built end to end on `claude/visionquest-job-mission-research-7pjair`, draft PR #204, HEAD `f801ed6` — VisionQuest goes from a job board into a placement broker, entirely dark behind flags.** Same day as the NLx/MACC research memo (`docs/plans/2026-09-04-nlx-macc-job-search-research.md`) and its follow-on design (`docs/superpowers/specs/2026-09-05-match-and-connect-design.md`) and six-phase plan (`docs/superpowers/plans/2026-09-05-match-and-connect.md`), the plan was executed phase by phase: one `builder` agent per phase in an isolated worktree (`.claude/worktrees/agent-*`, now gitignored), then a code-reviewer + security-auditor + database-architect + ux-reviewer pass per phase with every CRITICAL/WARNING routed back as a build instruction, then a second verification pass before the next phase started. CI green at every merge through `fd5bcf3`, `f35bb49`, `c339130`, `d07931e`, `32eed42`, `a24b922`, `8e18a7f`, the Phase 6 touch-up `59da793` (the retention doc block rewritten for the post-Phase-5 writer; `POST /api/teacher/connect/connections` now returns `includedFields` keys, with a new route test), a Phase 5 verification fix (moves the nudge run lock to transaction scope — `pg_try_advisory_xact_lock(2, …)`, replacing a session-level lock that measurably leaked across the pool and silenced the sweep after its first run — adds a 5-per-5-minutes fail-closed rate limit to the phone-verification-code confirm route, salts `hashVerifyCode` with the student id, adds a weekly-roster ceiling plus a `take: 800` cap, and adds `SmsReconsentNotice` to Home for students whose SMS consent was not carried forward), and this file's own prior docs update, `4a4ed69`. The final commit, `f801ed6` ("Second verification batch"), was still pending its own CI run at the time this was written: the nudge sweep now honours a wall-clock deadline (its transaction timeout minus 15 seconds) and stops between sends reporting `skipped: "deadline"` rather than running past it, and a finished sweep whose own final commit fails still returns its counts instead of losing them; the weekly roster query now intersects `sms_nudges_enabled_classes` with `connect_enabled_classes` in its `where` clause via `intersectScopeClassIds()` (either flag off yields `[]`, never a partial roster); `withPoolDefaults()` now applies to `ADMIN_DATABASE_URL` too (`connection_limit=10&pool_timeout=10`, overridable via `ADMIN_DB_POOL_SIZE`/`ADMIN_DB_POOL_TIMEOUT`, with any pool values already on the URL winning); `SmsReconsentNotice` moved out of `DashboardClient` (which the teacher's student-detail view also renders, so it had been briefly visible to teachers) into the student dashboard's own server page, with dismissal now via `useSyncExternalStore` over `localStorage`; the confirm-route rate-limit refusal is a `429`; and `normalizedPhone` now lives in `sms-policy-shared.ts`. Local gate before each merge was `prisma validate`, `eslint`, `tsc` (with `tsconfig.tsbuildinfo` removed first, per the standing cache gotcha), `platform:validate`, `ui-copy:readability --gate`, targeted `node:test` runs, and `npm test` (the 13 post-response logger failures are a known container artifact, green in CI — see Known Issues), plus a `next build` with the CI env block. At least 257 files changed, +41,988/-321 (measured before the Phase 6 and final Phase 5 fixes landed, so the true final count is somewhat larger). Five new migrations (`20260905100000` student work profile, `20260905110000` employers and job leads, `20260905120000` connections, `20260905130000` SMS consent and nudges, `20260905140000` the connect-nudges cron job) apply on the next Render deploy; the dev DB still needs `prisma migrate deploy` run by hand. Nothing is reachable in production today: `connect_enabled_classes` and `sms_nudges_enabled_classes` are unset (off), `connect_subsidy_lines_enabled` is deliberately left off pending WV Works sign-off (P0.8), and the connect-nudges cron job depends on #187's `app.base_url` GUC, which is still an owner step. See "Last Session" below for what shipped per phase and the day-one owner steps.

**2026-09-03 Wave 1+2 squash-merged to `main` (Cursor session finishing the Claude Code credit-stop).** F2/F3/F4 were already on main (#188 `743537e`, #186 `6b2c888`, #185 `2a73b5d`). Then: #197 F5/F62 (`44fcaa1`), #191 F7/F51 (`bc45dc3`), #190 F11 (`f9b2c36`), #196 F10 (`32b8ce6`), #193 F9 (`3088092`), #202 F66 (`0d57137`), #198 F13/F14/F19/F59 (`974ee6c`), #192 F26 (`3c1e7f7`), #194 F27 (`1dbd024`, migration `20260902140000_add_appointment_scheduled_slot_unique`), #200 F20/F21 (`7dd3d4e`), #201 F22/F23/F25 (`5f31c00`), #199 F23-UI/F24/F36/F45 (`41c6d96`), #195 F28/F29/F60/F61 (`f835b10`). **#187 (F1) stays draft** until the prod GUC and D4. **#136 stays open.**

**2026-09-02 critical tier (F1–F4) built, reviewed, hardened; F2/F3/F4 squash-merged to `main` on Britt's instruction (#188 `743537e`, #186 `6b2c888`, #185 `2a73b5d`); #187 (F1) is draft and waits on Britt's production steps.** Orchestrated as four `builder` agents in isolated worktrees, one finding per branch, then a review pass per branch (code-reviewer + security-auditor for F2/F3/F4; database-architect + code-reviewer for F1) with every WARNING routed back to the builder as a build instruction. All four are draft, mergeable, CI green. **#188 F2** (`bug/crisis-staff-notify-rls`, 8 commits): staff recipient reads and staff Notification read+write go through `prismaAdmin` via `{ client: "admin" }` on `sendNotification*`; the admin path resolves the recipient's role and refuses non-staff (fail closed); an empty recipient set logs `wellbeing_no_recipients`; same fix in `advising-interventions.ts`; `ADMIN_DATABASE_URL` added to `.env.example`; 4 RLS integration cases (CI ran 21/21). **#186 F3** (`bug/hide-staff-alerts-from-student`, 4 commits): `src/lib/student-alerts.ts` allowlist `["overdue_task","missed_appointment"]` behind both student pages and the Home next-step engine; ESLint `no-restricted-syntax` forbids direct `studentAlert` reads in student-facing globs. **#185 F4** (`bug/crisis-scan-before-provider`, 4 commits): never-throwing `src/lib/chat/crisis-scan.ts` awaited at the top of `POST /api/chat/send` for student chat, before direct answers, provider resolution, and rate limits; the post-response scan is removed (single call site); atomic per-student cap of 3 records / 10 min on the record, fail-open; `sourceId` now null. **#187 F1 code side** (`fix/pg-cron-baseline-reregister`, 10 commits): migration `20260902120000_reregister_baseline_cron_jobs` (guarded pattern, job commands byte-identical to the baseline, exercised on an ephemeral stubbed Postgres, applied in both CI databases), `npm run cron:health` (cron.job + job_run_details + `net._http_response`), `npm run jobs:expire-stale` (the D4 lever, dry-run by default), nightly `cron-health.yml`, runbook section "Repair, 2026-09" with the owner SQL. **#187 must not merge until the `app.base_url` GUC is set on prod and D4 is decided** (`job-processor` drains 20 rows per 10 minutes from its first success). Today's re-check of prod aggregates was denied by the permission classifier; yesterday's numbers stand.

**2026-09-01 full review complete** (`docs/audits/2026-09-01-full-review.md`, 59 findings F1–F59, 6 owner decisions D1–D6, plan in 7 bundles). Two production facts nobody could see from the repo: (1) **the scheduled layer has never worked in prod** — `cron.job` holds only 3 jobs (the 4 baseline jobs were never registered), all 3 fail every run with `unrecognized configuration parameter "app.base_url"` (runbook step 3 never applied), `BackgroundJob` has 153 pending since 2026-05-14, and `cron-health-monitor` is one of the unregistered jobs; (2) **crisis staff notification is dead under RLS** — the crisis path resolves teachers and inserts their Notification rows inside the student's RLS context, which the policies reject, and the student then sees the staff triage card on their own Advising page and Home. No crisis event has happened in prod yet (0 `wellbeing_concern` alerts). Same root cause across daily-coaching/alerts-sync/reports crons, teacher archive/reassign audit writes (500 after commit), grant-KPI history, and the coordinator role. Local dev connects as `postgres`, so none of it reproduces. Nothing was committed; the audit doc and this memory edit are uncommitted on `main`.

**PR #173 "doctor follow-ups" MERGED 2026-08-21 14:56 UTC** (squash `a7cd835`, merged by Britt; built on branch `claude/visionquest-doctor-xu6n2v`, afternoon session): (1) `npm audit fix` non-force, 17 → 6 vulnerabilities (remaining: the Prisma-7-gated deepmerge-ts chain ×3 high + one build-only fast-uri/ajv pair npm won't dedupe); (2) the 11 unmapped PLATFORM_MAP routes/tools mapped (validator 35/35/35, 0 errors) with `SAGE_PROMPT_REVISION` → `2026-08-21.1` for the +1.1k-char student prompt growth; (3) **the crisis-detection re-cut** from #136/#139/#141 — 13 new patterns (EN informal + pills/overdose means, ES pills/sobredosis), `lang`-tagged patterns, `CRISIS_RESOURCE_BLOCK_ES` selected by matched-pattern language — hardened by a two-reviewer pass (code + security; both *executed* the patterns: ReDoS-cleared linear ×13–18, zero CRITICALs) and a 7-fix follow-up commit with 18 red-baselined guard fixtures: `ksy`→`kys` (the typo was test-pinned), kms-kilometre guard (closing the one lang-contract leak — "caminé 5 kms hoy" had routed an ENGLISH 988 block), EN overdose first-person frame, **ES verb-register parity** (adherence talk like "me tomo todas las pastillas cada mañana" no longer alerts — all 15 ES must-detect rows preserved), `end things with` breakup guard, hair-lookahead `\b`, ES clitic variant. CI fully green on first push including the gating evals on the refreshed credits (the new 32nd red-team case `crisis-informal-contraction` executed for the first time and passed). **#139 and #141 closed as superseded ~15:00 UTC the same day**, each with an evidence comment naming where its content lives on main.

**PR-queue sweep, 2026-08-21 ~15:00–15:30 UTC (owner-authorized)**: #134 and #154 CLOSED with evidence comments (red-CI scaffold superseded by #158's mechanisms; `.cursor/` config vs the machine-local precedent). #135 CLOSED after salvaging its one standalone piece as **PR #175** (`fix/salary-period-parsing`, byte-identical port of `ce2aeca`'s salary-parser + tests, red-baselined 6-fail→24/24 — the bug showed a $400/week job as $400/hr through six adapters). **Both salvage PRs MERGED 2026-08-21 ~16:25 UTC after an independent review pass** — #175 (`3116663`) and #176 (`143e2a5`). #131 CLOSED superseded by **PR #176** (`fix/staff-mfa-settings-reachable`, fresh re-cut on current main): the bug was verified still live — StaffMfaPanel rendered only under `(student)/settings`, whose layout redirects staff, so teachers could never reach MFA settings and #158's staff-settings copy was dead code; fix moves the page to a shared `SettingsView` mounted in both route groups with role-aware links (NavBar ×4, ChatErrorBanner via prop) and `/teacher/settings` in the validator's `ROUTE_IGNORE`. The pre-merge review (zero CRITICALs; it verified #175's byte-identical port by **blob SHA** and #176's three auth gates in agreement) produced one must-fix, shipped before merge: the settings move newly exposed `ConsentSection` to staff, where it half-works — `api/chat/upload` gates the cloud path on `role === "student"` so flipping it changes nothing there, while `classify_attachment` DOES honor it for the staff member's own files, so the copy promised more than it delivered. Now student-only, red-baselined; the session-role fallback also no longer demotes a teacher to "student" on a degraded session response (which would hide their own MFA panel). **The #136 re-scope audit is DONE** (26 of 29 findings STILL LIVE on main — see Open Items).

**PR #164 merged 2026-08-21 13:32 UTC** (`0ce2c05`, by the concurrent morning session after the credit-restore re-run went green): student identifiers out of every server-log payload, CI-gated by the ESLint `no-restricted-syntax` rule. Standing review question: is `studentLogKey` correlation enough for wellbeing/crisis debugging? **PR #171** (per-role local AI models, draft, awaiting Britt's bake-off) was conflict-resolved with main (merge `aa75a60`) and got its first CI run.

**Per-role local AI models (2026-08-21, branch `claude/visionquest-local-ai-roles-4bdcdr`)**: VisionQuest now routes each generative AI job to its own local model. Four roles — `chat` (interactive + tools), `extract` (strict JSON over transcripts), `document` (long messy input to JSON), `draft` (sustained prose) — hang off the `AiTask` every call site already declares, so all 11 `resolveAiProvider` sites are role-routed with no call-site edits. Config: `ai_provider_model_{role}` + `ai_provider_max_output_tokens_{role}` (env fallbacks too); unset = today's behavior exactly. **No model is assigned to any role yet** — that needs one bake-off run on the real host. New instrument: `npm run sage:model:bakeoff -- --models=a,b,c`, plus `--model=` on the shared eval chokepoint so all six model-driving evals gained it at once. Decision doc: `docs/plans/2026-08-21-local-ai-role-models.md`.

**PR #158 merged 2026-08-20** by Britt (`c21189a`, 37 commits): the full charter Phases 1–5 execution — instruments, one-path workflow fixes, living-Sage freshness/memory, student-reported CareerOneStop pathways + 14-cluster migration, trust surfaces, the 48-string copy rewrite, and post-review hardening incl. migration `20260820140000` tightening `sage_operation_read` RLS (new + student-actor rows only; legacy staff-actor rows have no recoverable student link).

**Draft PR #163 now targets `main`** (branch `bug/welcome-step2-moving-target`, worktree `elegant-rhodes-8ad3b6`, 4 commits): welcome step-2's navigation no longer unmounts mid-tap while the last quick-win save is in flight, the score card's space is reserved so revealing it cannot move the row beneath it, and the 2s auto-advance is revocable so it can never override a step the student chose. Verified in a real browser at 375px — 0px of movement, same DOM node across the save — plus 2516/2516, tsc/eslint/readability green. It began stacked on #158 and was retargeted when #158 merged; the rebase onto `main` conflicted only here, in this file.

PR #156 (Sage ledger attribution, from this session's spawned chip) is merged to main: `SageOperation.targetStudentId` + migration `20260820120000` (Render applies it to prod on deploy), with `recordOperation`/write-tool threading so staff on-behalf-of writes carry the student link. The read side (viewer OR-clause + RLS tightening) shipped with #158.

Journey hardening merged as PR #153, so the placement bridge, evidence-reporting fixes, and journey a11y are on `main` (bridge still default-off — enable per class via `/api/admin/placement-bridge`).

**PR #159 merged 2026-08-20** (`d8cf43b`, squashed): the deferred percentile-consolidation chip, done. `scripts/lib/percentile.mjs` is now the only percentile implementation — **four** scripts had their own, not the three the chip listed (`sage-rag-harness.mjs` carried a fourth as an inline closure). The p argument is 0–100 everywhere and out-of-range p throws, so the old 0–1 decimal convention now fails loudly instead of silently reporting a near-minimum as a median. Also retires the deprecated `mock.module` `namedExports` option in `post-response.test.ts`.

The Sage confirm-token hardening is fully shipped: **#156** (write-tool ledger rows attributed to the acted-on student), **#157** (confirm tokens signed over canonical payload JSON instead of a `"|"`-joined field list, `fce8e5b`), and **#160** (the `propose_resume_edit` confirm card carries `targetStudentId`, which its token already bound, `6f4e62f`) all merged 2026-08-20. #157 and #160 were built on separate branches and only interact once merged, so merged `main` was verified as a combination rather than per-branch: 2074/2074 tests, eslint and tsc exit 0.

**#157 invalidates in-flight confirm tokens on its first deploy.** Bounded by the 10-minute `TOKEN_TTL_MS` and fail-closed (verification fails, nothing executes), so the worst case is a student who saw a card just before the deploy and confirms just after having to ask Sage again. Nothing to do — recorded so the symptom is recognizable in the deploy window.

Stable and shipped through PR #149 (2026-07-28). Nothing is awaiting Ship — #138, #142, #143, #145, #147, #148, #149 are all merged, and both prod and dev have the `ProgressionEdge` table seeded (12 edges each, read-back verified, idempotent on re-run), so the static fallback is no longer the live path in either environment.

The Sage local-eval chain is green: `sage:quality:eval --provider=ollama` returns **8/8 replies, avg FK 5.1, 0/8 over the grade-8 target**. Getting there took three separate fixes to the same `""` symptom — reasoning eating the output budget (#147), an undeclared tool call stripped by Ollama's built-in gemma4 parser (#149), and a stale-worktree Prisma client. The mechanisms are in Known Issues; the reasoning behind each choice is in the Key Decisions Log. One provider-side hole remains open (see Open Items): `generateResponse` still returns `""` silently when a turn ends in a tool call.

Shipped workflow surface: `/ci-pipeline` (#121–#125) and `/a11y-pipeline` (#129), both CI-enforced through the umbrella `pipelines:validate` gate; `/teacher/failed-extractions` is in the teacher nav as "AI Review" (#128). Career-grounding Phase A is locked (#133); Phase B upload/sync is owner-governed and still blocked on the Windows machine.

**Merge-flow gotcha**: verify merges against the GitHub API, not against reports — silent merge failures happened twice before the `Bash(gh pr merge *)` allow rule was added to Claude's project-local settings.

Prior state stable: eval-gate stabilization (#118), maturity repair deployed (#117); see `docs/MATURITY_REVIEW.md`.

## Last Session
- **Date**: 2026-09-05
- **What we worked on**: Britt asked for the missing piece from the 2026-09-04 research memo — a tool that matches a student to an employer and brings them together — built as a full six-phase initiative on one branch, orchestrated across isolated worktrees. **Phase 0** (same day as the research memo, ships dark): the WorkForce WV ("West Virginia Employer") pass in the CareerOneStop adapter, the `JobCard` badge and MACC apply hint, and `cos:smoke` job-board families to verify both once credentials land. **Phase 1** (job supply): the Talroo adapter with `fetchJson` hardening and a `talroo:smoke` script; fetch timeouts added to `jsearch`/`usajobs`/`adzuna` (VQ-R-019); `DEFAULT_JOB_SOURCES` updated and a teacher nudge when WV Local Jobs is unconfigured. **Phase 2**: `StudentWorkProfile` (student-owned, RLS-scoped) fed by a five-question Sage intake (`update_work_profile`) or a Settings form; Sage tools `search_jobs` and `explain_job` (read-only, `read_ai` tier, grounded in real postings, wage figures re-verified after generation, readability-gated at grade 6) plus a `ReadAloudButton`. **Phase 3**: `Employer`, `EmployerContact`, and `JobLead` models (staff-only RLS, students read only their class's `JobLead` rows), a backfill script from existing `Opportunity`/`SpokesRecord.employerName` strings, the `fit()` matching engine (hard blocks plus the existing sub-scorers), and the `/teacher/connect` job developer console with a WorkForce WV batch export. **Phase 4** — the core of the initiative: `Connection`/`ConnectionEvent`/`OutboundMessage` models and the `propose → student approves → instructor sends` flow behind `confirmationGate`; the public `/connect/[token]` employer response page (no student id ever in the URL); a hire creates a verified `Application` and fires the placement bridge; local-only endorsement drafting from verified facts; the WV Works subsidy table, gated off pending P0.8. **Phase 5**: SMS consent with phone verification, an hourly `connect-nudges` cron job (`src/lib/nudges/`), a Twilio inbound webhook handling STOP/START/Y/N, and six new staff alert types (employer no-view, no-response, interview unconfirmed, retention confirm/lost/unanswered) plus one new student-visible type (`connect_weekly_jobs_ready`, added to `STUDENT_VISIBLE_ALERT_TYPES`); two verification-pass fixes, CI green through this file's own docs commit `4a4ed69`: first, a fix that moved the run lock to transaction scope after a session-level lock was measured leaking across the pool and silencing the sweep after its first run, rate-limited the phone-verification-code confirm route (5/5 min, fail-closed, `429`), salted `hashVerifyCode` with the student id, added a weekly-roster ceiling plus `take: 800`, and added `SmsReconsentNotice` to Home for legacy SMS opt-ins; then `f801ed6` ("Second verification batch", pending CI) added a wall-clock deadline to the nudge sweep (its transaction timeout minus 15 seconds, stopping between sends with `skipped: "deadline"` rather than running past it, and still returning its counts even when the sweep's own final commit fails), made the weekly roster query intersect `sms_nudges_enabled_classes` with `connect_enabled_classes` via `intersectScopeClassIds()` (either flag off yields `[]`), extended `withPoolDefaults()` to `ADMIN_DATABASE_URL` (`connection_limit=10&pool_timeout=10`, overridable via `ADMIN_DB_POOL_SIZE`/`ADMIN_DB_POOL_TIMEOUT`, explicit URL values win), moved `SmsReconsentNotice` out of `DashboardClient` (which the teacher's student-detail view also renders) into the student dashboard's own server page with `useSyncExternalStore`-based dismissal, and moved `normalizedPhone` into `sms-policy-shared.ts`. **Phase 6**: the Connect funnel report (`/teacher/connect/report`), a DoHS-facing CSV export (columns are VisionQuest's best guess pending P0.4(1)), and `pathway-outcomes` promoted from script-only to a route; a touch-up commit (`59da793`) rewrote the retention doc block for the post-Phase-5 writer and made the connections route return `includedFields` keys, with a new route test. `SAGE_PROMPT_REVISION` bumped to `2026-09-05.5` across the Sage-tool additions.
- **What we decided** (owner-behalf calls made under the veto-window pattern, all reversible, all named in the PR body): a student's packet strips the résumé's contact block down to a name header when sent to an employer, rather than changing the consent label (SEC-W4); an SMS "still working" retention reply never writes `SpokesEmploymentFollowUp` directly — it advances the Connect pipeline and raises a `connect_retention_confirm` staff alert for a person to record the official DoHS/WIOA checkpoint, because the two records use different clocks (30/60/90 days vs 1/3/6 months) and could otherwise silently overwrite a teacher's verified row; existing SMS opt-ins are not backfilled — a checkbox nobody ticked for the new consent language is not consent, so previously-opted-in students (including the daily coaching text) go quiet until they re-confirm with a phone-verified code; the DoHS export's student identifier column is `Student.studentId`, the same login id `credentials/[slug]` already prints publicly (opens a second instance of F12, tracked in Open Items) rather than inventing a non-authenticating report id ahead of knowing DoHS's real field list; `interested` is a real resting connection state, not a transient one, with legal edges from `sent`/`viewed` straight to `hired` for when an employer skips the interview step; the DoHS export is treated as a program-administration disclosure needing no per-student consent scope, stated in the export module's header for the next reader to check; SMS phone verification (a one-time code) is required before `smsConsentAt` is stamped, so a typo cannot enroll a stranger's number; advisory locks in pooled code are always transaction-scoped (`pg_*_xact_lock`) with distinct class ids, never session-scoped, after the nudge runner's session-level lock was reproduced leaking (one leaked lock, then nine skipped runs in a row).
- **Where we left off**: PR #204 stays draft pending Britt's review — the design doc's five open decisions (spec §12: packet contents, who may send, the consent instrument, employer-page branding, retiring `Opportunity`) are still hers to make, and the owner-behalf calls above are a veto window, not a final answer. Branch head is `f801ed6` ("Second verification batch"); CI is green through `4a4ed69`, and `f801ed6`'s own run was still pending when this was written, so confirm it went green before treating the branch as fully proven. Day-one steps before anything goes live: set `COS_USER_ID`/`COS_API_TOKEN` and `TALROO_API_KEY` (`.env.local` + Render), run `cos:smoke` and `talroo:smoke`; set `TWILIO_AUTH_TOKEN` and the sending number in Render and point the Twilio console's inbound webhook at `/api/sms/inbound`; open `connect_enabled_classes` and `sms_nudges_enabled_classes` for a pilot class (`connect_subsidy_lines_enabled` stays off); run `prisma migrate deploy` on the dev DB; confirm `ADMIN_DATABASE_URL` is set everywhere the nudge runner and inbound webhook run, or they refuse to act by design (the F63 boot probe now covers both, `rolbypassrls` check via `prismaAdmin`); the `connect-nudges` cron job depends on #187's `app.base_url` GUC, still outstanding. No Sage agent-eval or red-team scenario exists yet for `propose_connection` or `search_jobs` — needs a `GEMINI_API_KEY` in CI to author safely, left for a follow-up session. P0.4(1) (the real DoHS field list) still needs to come back from WVDE before `DOHS_EXPORT_COLUMNS` can be trusted as more than a best guess.

## Prior Session (2026-09-03)
- **Date**: 2026-09-03
- **What we worked on**: Finished the 2026-09-02 critical-fix wave Claude Code left in worktrees after credits ran out. Closed leftovers: #197 audit silent-gap (`{ audited }` + `alert: audit_write_failed`); F66 password-reset MFA (#202); pushed two unpushed #195 commits, then rebased #195 onto main so it kept both the confirm-card refund and #198's `rateLimitsDisabled` predicate. Squash-merged Wave 1 (#197, #191, #190, #196, #193, #202, #198) and Wave 2 (#192, #194, #200, #201, #199, #195). Left #187 unmerged.
- **Where we left off**: #189 (this file + the audit doc) is the last merge of the wave. Owner still owns Bundle 0 / F1 (#187): GUC, D4, `CRON_CHECK_DATABASE_URL`. Product calls D7/D8 and D1–D6 remain. Bundles 4–6 and leftover findings (F6, F8, F12, F15–F18, F30+, F63) were not in this pass.

## Prior Session (2026-09-02)
- **Date**: 2026-09-02
- **What we worked on**: Britt asked to fix the review's critical findings, orchestrator-style with sub-agents. Four `builder` agents in isolated worktrees (`.claude/worktrees/crit-f{1,2,3,4}-*`, each with its own `npm ci` + `prisma generate`), file-fenced so F2 (crisis-detection/notifications internals) and F4 (route/post-response call site) could not collide. Then two reviews per branch; eleven review findings were fixed in-branch before anything was called done (F2: admin-path staff-role check, no-recipient alarm, RLS regex narrowed to `/row-level security/i`, production-join case; F3: ESLint drift rule; F4: null `sourceId`, per-student cap; F1: `net._http_response` in the health check, catalog-based GUC verification instead of `SHOW`, corrected migration-header causality with the restore hazard, `_prisma_migrations` pre-check, workflow `permissions`). One mechanism correction from CI: under student context the enrollment→instructor join does not return zero rows, it throws Prisma's inconsistency error, which `resolveWellbeingRecipients` already catches; same outcome the review described.
- **What we decided**: allowlist over denylist for student-visible alert types (fails closed); one crisis-scan call site with an explicit atomic cap rather than re-scanning in post-response; bound the admin client at the helper (runtime staff-role check), not only at call sites; the cron migration stays unconditional (registering inactive or GUC-gating would recreate the applied-but-never-registered trap), ordering lives in the runbook. Two new decisions for Britt: **D7** (six neutral alert types now hidden from students) and **D8** (teacher nudges program-wide vs assigned instructors). Adjacent findings recorded as **F60–F63** in Open Items.
- **Where we left off**: Britt said "Merge #188, #186, and #185"; all three squash-merged ~13:54 UTC and API-verified, their worktrees and branches removed, local `main` at `2a73b5d`. Post-merge CI on `main` is the first run of the three fixes together (they touch disjoint files; each branch was green against `c9913dd`). Still open: #187 (F1) waits on runbook "Repair, 2026-09" step 1 (GUC) and D4 (step 2), then merge, then step 4 verification and `npm run cron:health`; #189 (this memory + the audit doc, branch `docs/2026-09-01-review-and-critical-fixes`). Worktrees `crit-f1-cron-reregister` and `docs-review-2026-09-01` remain registered under `.claude/worktrees/`; remove after their PRs merge.

## Prior Session (2026-09-01)
- **Date**: 2026-09-01
- **What we worked on**: Britt asked for "the works" — a complete review of VisionQuest. Synced local `main` (was 31 commits behind), re-baselined (2,860/2,860 tests, lint clean, CI green), then 14 read-only review passes (security, Sage core, Sage memory/retrieval, staff routes, student routes ×2, domain logic ×2, DB, frontend, UX, tests/CI, product inventory, ops) with orchestrator spot-verification of every claim that made the top tier, live probes of production (headers, CSRF, 375px landing page), and aggregate-only SQL against the prod Supabase project (cron.job, job_run_details, BackgroundJob status, Notification/StudentAlert type counts, RLS-disabled tables, role flags). Report: `docs/audits/2026-09-01-full-review.md` (+ published artifact).
- **What we decided**: nothing — the review lists six decisions for Britt (D1 FERPA routing policy, D2 coordinator build-or-freeze, D3 job-board unification, D4 drain-or-expire the 153 pending jobs, D5 delete list, D6 close #136 and re-cut). Agent severities were recalibrated where inflated (confetti, export memory, readiness div-by-zero) or understated (crisis path).
- **Where we left off**: Bundle 0 (prod repair: `ALTER DATABASE postgres SET app.base_url`/`app.cron_secret`, re-register the 4 baseline pg_cron jobs, decide D4, schedule the detectors) is owner work and the first thing to do. Three prod queries were denied by the session's permission classifier (full `cron.job` command text, Notification counts by recipient role, `_prisma_migrations` ledger vs main) — two-minute checks for Britt. Prod also has a `CareerAssessmentSnapshot` table (no RLS) created by `20260724140000_add_interest_profiler_provenance`, a migration applied to prod from `feature/apify-job-sources` that exists on no main migration.

## Prior Session (2026-08-21)
- **Date**: 2026-08-21 afternoon (remote session, branch `claude/visionquest-doctor-xu6n2v` → draft PR #173 — a "/doctor" health check, then executing its findings with a six-agent Opus team after Britt refreshed the Gemini wallet ~$9)
- **What we worked on**: (1) A 10-probe doctor run at `bbbaaaf`: every local gate green, unit suite exactly at the container baseline, MEMORY.md 9/10 claims confirmed with one DRIFT — the recorded crisis gap was overstated (method-adjacent cutting patterns existed; what was missing was means-specific pills/firearms/hanging/jumping and the Spanish 988 block). New facts surfaced: `platform:validate` exits 1 correctly but is wired into NOTHING (how 11 unmapped entries drifted in); npm audit at 17 vulns/8 high; unrecorded draft PR #171 sitting conflicted with zero CI runs. (2) Execution: #171 unblocked (merge `aa75a60`; the #171 session pushed `f3f23bf` mid-merge — agent detected the non-ff rejection and redid the merge on the new head), platform-map filled, crisis re-cut built from the three parked PRs (all three applied to main conflict-free; red-baselines: 21+19+6 failing assertions per source set), then a code-review + security-audit pass and a 7-fix commit (see Current Status), plus the npm audit fix. #164 was merged by the concurrent morning session while this one ran — verified via API, not assumed.
- **Where we left off**: PR #173 draft, CI green on its first push including gating evals; the final push adds the review fixes and this memory update. Branch cleanup is blocked from remote sessions (see Known Issues) — 37 verified-safe branches await Britt's one-liner (Open Items). Deferred with homes: firearms/hanging/jumping means family (BOTH languages — now the largest crisis-coverage gap), VQ-R-001 crisis-scan-before-provider audit, Spanish informal-register native review, platform validator extensions, wiring platform:validate into CI. The prismaAdmin lint rule remains the last unclaimed #158 follow-up.

## Prior Session (2026-08-21 morning — resume + button-up + #164 merge)
- **Date**: 2026-08-21 (remote session, branch `claude/resume-previous-work-2y8gs1` → PR #169 — resumed after the computer-update pause)
- **What we worked on**: Picked the work back up by reconciling this file against GitHub. Found the late-2026-08-20 burst not yet recorded here: **seven PRs #162–#168 open, zero reviews, all awaiting Britt** — #162 (mobile journey strip, closes chip task_73fdd22f; CI green, retargeted to main), #163 (welcome step-2 moving target; draft, green), #164 (student-ids-out-of-logs + lint; draft, **merge-conflicted with main**, no CI run), #165 (repo-wide `dark:` variant fix; stacked on #162, CI runs only once retargeted to main), #166 (memory docs for #159 — supersedes this file's percentile open item), #167 (Sage XP freshness; draft, green), #168 (atomic rate-limit counter; green — asks an explicit fail-open-vs-closed call). Then built the unclaimed #158 follow-up: **PR #169, confirm-card tokens single-use** (see Open Items + decision log). Route tests were written first and observed failing on the pre-claim route (replay returned 200 and executed twice), 27/27 confirmation tests after; eslint/tsc/prisma-validate clean; full suite 2532/2545 with the 13 failures byte-identical on clean main in that container (post-response logger artifact, CI green on the same commit).
- **Where we left off**: Britt then asked for the full button-up, so the same session ran the merge sweep: **#166, #167, #168 squash-merged; #162 merged with a merge commit after resolving its MEMORY conflict — which auto-merged #163** (its commits were contained; the branch also carried Britt's settled call: score card holds 8s, `SCORE_ADVANCE_DELAY_MS` floor test). #165 retargeted to main + main merged in (fires its first CI). #164's 39-file conflict resolution delegated to an agent (its lint rule re-verified against everything merged today). #169's `sage-memory-eval` failure was flake at the 90% floor (89.3%, misses in subsystems the diff never touches; green on #167/#168 same day) — confirmed by the one allowed re-run, then merged. **#168 shipped the fail-open rate-limiter default its PR flagged for owner review — Britt should confirm or flip it.** #169's migration still needs dev-DB `prisma migrate deploy` (remote container had no DATABASE_URL; the lone Supabase project is prod — do NOT apply via Supabase tooling, it bypasses Prisma's ledger and would break the next deploy). Final sweep state: **eight PRs merged in ~35 minutes** — #166, #167, #168, #162 (+#163 auto-merged via contained commits), #165, #169, #130 — plus #132/#150/#155 closed as superseded with evidence comments (12-agent audit, dispositions in Open Items). #169's two eval failures were both confirmed flakes by single re-runs (memory retrieval 89.3% vs the 90% floor; red-team `fabricate-action` banned-string "has been submitted" firing on a CORRECT refusal — that scenario needs a scenario-side tightening, owner's call under the frozen-grader rule). Then the eval account ran dry: **Gemini prepaid credits depleted mid-sweep**, which is what actually blocks #164 (see Open Items — branch is done and verified, only its credit-starved gating evals are red). The prismaAdmin lint rule remains the last unclaimed #158 follow-up.


## Prior Session (2026-08-20 — percentile consolidation, shipped #158/#159)
- **Date**: 2026-08-20 (worktree `charming-tu-b911ff` — percentile consolidation → PR #159, then shipped #158 and #159)
- **What we worked on**: Took the deferred percentile chip. Found **four** implementations, not the three the chip named — `sage-rag-harness.mjs` had an inline-closure fourth on the same 0–1 convention, and leaving it would have preserved exactly the hazard the extraction removes. Extracted `scripts/lib/percentile.mjs` (0–100, throws out of range, `null` for empty) TDD-first, red-baselined the swap against all four originals over a boundary fixture, and modernized the 22 deprecated `mock.module` `namedExports` calls in `post-response.test.ts` — the Node 24 deprecation warning was confirmed present before and absent after, against a control copy. Then shipped both open PRs: **#158 merged** (`c21189a`, merge commit — 39 curated commits kept for bisection per the #117 precedent) and **#159 merged** (`d8cf43b`, squashed — one logical change plus a bookkeeping merge not worth keeping).
- **Also found**: three environment traps, all now in Known Issues. (1) The worktree was borrowing the main checkout's `node_modules`, so tsc showed 6 phantom Prisma errors and forms-delivery failed on a missing tsx — `npm ci` in the worktree fixed both and left the main checkout's client untouched (verified by mtime). (2) `tsconfig.tsbuildinfo` cached tsc results and made a with/without comparison report 0 errors both ways; the real answer was 0-vs-22. (3) `gh pr merge` printed **nothing at all** on success for both PRs — the existing merge-flow gotcha is real, and every merge here was confirmed via the GitHub API plus a content check on `main`, not from command output.
- **Where we left off**: `main` at `d8cf43b` + this docs commit. #158 needed a `main` merge mid-flight (#157/#160 landed and conflicted it) — conflict was `.claude/MEMORY.md` only, resolved by keeping both sides, code auto-merged. Branches `claude/visionquest-review-improve-1ac878` and `claude/charming-tu-b911ff` are merged and safe to delete; the sibling worktree still sitting on the former needs a `git pull` before reuse. Chip task_48c0f870 can be dismissed.

## Also 2026-08-20 — log-PII sweep, PR #164 (concurrent session)
- **Date**: 2026-08-20 (worktree `sharp-panini-963729`, branch `claude/sharp-panini-963729` → draft PR #164)
- **What we worked on**: A security audit flagged one line, `recordStudentView`'s failure path logging a student id. Fixing it exposed 38 more sites and a self-contradicting rule: `.claude/skills/student-data-privacy` said "use student IDs, never names/emails" in logs, sanctioning exactly what the audit flagged. Settled the standard in the skill and `.claude/rules/security.md`, then swept every site. Sites now log `studentLogKey(id)` (one-way sha256 prefix) rather than dropping the field, because most of these paths are logged precisely because nothing reached the DB, so the log line is the only trace. Added `redactContactInfo` after finding SMTP and Twilio errors quote the address and number inside the error text, which put back the PII the payload had just removed. The durable artifact is a `no-restricted-syntax` ESLint rule that generated the target list, verified the fix, and now gates in CI.
- **What we decided**: Student ids are PII in logs, same as names and emails (settled a doc contradiction, not a new rule). Correlation keys beat deletion on DB-write-failure paths. No salt on the digest, because cuids are high-entropy and unenumerable. `sms.ts` was fixed alongside `notifications.ts` since fixing only the caller would have been cosmetic. `conversationId` was deliberately kept.
- **Where we left off**: Draft PR #164 open, not merged, awaiting owner review. The codemod that performed the sweep lives only in the session scratchpad and was not committed; the ESLint rule is the artifact meant to outlive it. The duplicate session spawned from a task chip (`eloquent-chatterjee-239334`) was archived and its worktree removed after confirming its single commit `d370791` was strictly weaker (it kept `studentId` and never touched `sms.ts`); that commit is still reachable from its branch ref.

## Also 2026-08-20 — charter Phases 1–5 (concurrent session)
- **Date**: 2026-08-20 (worktree `visionquest-review-improve-1ac878` — charter Phases 1–5 executed by agent team)
- **What we worked on**: Britt approved executing ALL charter phases with two amendments: no COS API key yet → built the **student-reported CareerOneStop results workflow** (`record_assessment_results` confirm-card write tool, provenance fields on CareerDiscovery, Career DNA source labels, extractor stomp-guard), and **national clusters migrated 16 → 14** (Advance CTE modernized framework; legacy names normalize at every read path; idempotent backfill script). ~17 build agents + integration landed 31 commits on `claude/visionquest-review-improve-1ac878`: Phase 1 instruments (latency+SLO, freshness eval, career evals w/ watch tier, UI-readability gate, eval floors, e2e/a11y in CI, cos-smoke), Phase 2 workflow (orientation = journey step 0, one next signal, discovery dead-end unrepairable-state fix, welcome honesty, /help + retry, provider deadlines/stall guards, tool-call silent-empty closed), Phase 3 living Sage (write-through cache invalidation — strict freshness eval 6 FRESH gates CI; recent-activity prompt section; insight loop; teacher+operation memory; briefing soak tooling; welcome self-destruct root-caused: redirects inferred newness from the provider's own side-effect row), Phase 4 pathways (placement provenance + first pathway-vs-outcome report; teacher cluster picker; corpus triage workbench — 463-row worksheet generated), Phase 5 trust (AI accountability report — **FERPA flag fired on dev: ai_provider=cloud, 57/57 student_record calls cloud-routed**; student /memory page; SageOperation viewer; backup runbook — **Supabase backups exclude Storage at every tier**; load test — **15-student local-AI projection 9.0 min p50, past the 300s timeout**), and the 48-string copy rewrite (median 5.0, gate live).
- **Also found**: memory extraction was silently dead on the local FERPA path (json_object mode cannot emit the bare array the prompt demanded; 0 memories stored, zero errors — fixed, red-baselined). Peer branch PR #156 (spawned chip) added `SageOperation.targetStudentId`; coordination recorded (my viewer's OR-clause upgrade + RLS tightening = merge-time follow-ups). Mid-run incident: an agent's `git stash`/`pop` swept the shared worktree for ~1 min; fully recovered, destructive-git-ops rule now broadcast in every agent prompt.
- **Where we left off**: all three review passes done and their fixes committed (final: 2498/2498 tests, tsc/eslint/readability gates green); rebased onto main post-#156 (zero conflicts); **PR #158 merged 2026-08-20** (`c21189a`) — the CI verdict it was waiting on came back green. SAGE_PROMPT_REVISION 2026-08-19.3. Migrations `20260820130000` + `20260820140000` applied to local + dev. Deferred-with-homes: mobile 8-step-strip redesign (chip filed AND listed below, since chips don't survive app restarts) — the percentile-consolidation half of that pair shipped as #159; confirm-card nonce and the prismaAdmin lint rule are PR-note follow-ups.

## Also 2026-08-20 — confirm-token hardening, #157 (concurrent session)
- **Date**: 2026-08-20 (worktree `priceless-wright-bdfbd1`, branch `security/confirmation-hmac-canonical-payload` → PR #157)
- **What we worked on**: Hardened the Sage confirm-before-execute token signature. The HMAC input was the payload's fields joined with `"|"` and no length prefixes; the 2026-08-20 security-auditor pass on #156 found it safe today but fragile, with one live instance (`targetStudentId ?? ""` hashed absent and empty-string identically). Replaced it with canonical JSON of the whole payload through the existing `canonicalize()` helper. TDD throughout — six new cases, each watched fail against the old scheme first. A second security-auditor pass over the finished diff returned PASS with one WARNING and two SUGGESTIONS, all three fixed with red-baselined tests: the payload now sits under its own key so a future `ConfirmationPayload.expiresAt` can't be shadowed out of the HMAC, undefined array elements canonicalize to `null` like `JSON.stringify` (`[undefined]` had collided with `[]`), and keys sort by codepoint instead of locale-dependent `localeCompare`. 2063/2063 unit tests, eslint/tsc clean.
- **What we decided**: Invalidating in-flight confirm tokens is an acceptable cost rather than a reason to version the token format — the window is one 10-minute TTL and the failure is fail-closed. No `SAGE_PROMPT_REVISION` bump: no prompt text changed, and the revision exists for regression attribution on prompt behavior. Gating evals run in CI rather than locally (they need the `GEMINI_API_KEY` secret); the `src/lib/sage/**` path filter means `sage-evals.yml` does fire on this PR.
- **Where we left off**: #157 and #160 both squash-merged on Britt's instruction, verified against the GitHub API rather than the command output — which mattered: **`gh pr merge --delete-branch` exits 1 in a worktree checkout** because its post-merge local step cannot switch to `main` (already checked out elsewhere). It looks exactly like a failed merge and is not one. Check `state`/`mergedAt` before believing the exit code, and delete the remote branch separately. The gating evals ran live rather than no-opping on both — 31 red-team scenarios against gemini-3.1-flash-lite, "PASS: no hard boundary violations", guardrails 5/5 including the prompt-leak canary, tool selection 4/4.

## Prior Session (2026-08-19)
- **Date**: 2026-08-19 (worktree `visionquest-review-improve-1ac878` — full product review + "what better means" charter draft)
- **What we worked on**: Britt asked for a complete review of VisionQuest against their definition of "better" (guided workflow, Sage accurate/fast/reliable, living agent current with native changes, accurate pathways via CareerOneStop, easy to use). Ran a 15-agent workflow (8 dimension reviewers, 6 adversarial verifications, 1 completeness critic) at commit `2c52e3a`: 61 evidence-cited gaps, 5 criticals confirmed. Synthesized into a draft successor charter for the lapsed 90-day window: **`docs/plans/2026-08-19-what-better-means-charter.md`** (canonical, uncommitted in this worktree) + presentation artifact "What Better Means for VisionQuest" (https://claude.ai/code/artifact/6db6b115-4faf-47ed-ac10-689c79cfe9ff).
- **Headline findings**: (1) pathway chain dark — COS never connected AND assessed results have no persistence path (CareerDiscovery lacks a source field), so credentials alone won't fix it; (2) zero career answer-accuracy instruments (no "riasec/pathway/occupation/wage" string in any eval config); (3) living agent built-but-off — briefing loop dark (`SAGE_AUTOPILOT_ENABLED` absent from render.yaml), bundle events/alerts/insights queried each turn then discarded, SageInsight writer unwired, memory hardcoded student/conversation; (4) freshness blind spot — chat context caches (180–600s TTL) have no write-through invalidation and the DB→context read plane has zero real test coverage; (5) measurement asymmetry — safety/tool-routing gated, but grounding/latency/freshness/e2e/a11y/UI-readability unmeasured (UI copy samples FK 8.4–14.5 vs grade-6 audience; login hero 14.5). Completeness critic added: FERPA soft-switch with write-only audit log, no student window into Sage memory, no cost meter (+ personal Gemini keys resolve first), no backup/restore story, classroom concurrency of serial Ollama unassessed, teachers can't see Sage's routine guidance.
- **What we decided**: Nothing merged/committed — the charter is a DRAFT awaiting Britt's review. Phase 0 of its plan is all owner actions (COS credentials, DATABASE_URL secret for the drift job, grade-6 vs grade-8 ceiling, briefing-soak flip, personal-key policy, oversight scope, consent/retention).
- **Where we left off**: Charter awaiting owner review. New skills verified this session: pstack suite installed 2026-08-19 in `~/.claude/skills/` (reflect, recall, unslop, blast-radius, how/why/teach, show-me-your-work, create-verification-skill, etc.); MemPalace MCP is live and backfilled but its `visionquest` room is duplicate-heavy (same PRODUCT_GUIDE chunk from 3 worktrees = all top-8 hits) — low-precision recall until curated.

## Previous Session
- **Date**: 2026-07-31 (review + harden + transfer of the BuzzProjects sandbox agent-team work → branch `ops/journey-hardening-2026-07`, merged as PR #153)
- **What we worked on**: Formal review of the three commits an agent team produced in the `/Users/brittlegg/BuzzProjects/VisionQuest` sandbox (evidence reporting c7b4035, journey a11y 2d236b5, placement bridge 1dc7be5). All three rated adopt-with-changes; three parallel fix agents hardened them in sandbox worktrees (readiness denominator reverted to the all-items convention, ready-to-work soft-match fallback, portfolio ARIA repair, dead-anchor pruning + integrity tests, placement link⇒date invariant, queue action wiring, admin flag API, migration resequence). A second review gate found one CRITICAL — the new queue actions deep-linked `#spokes-profile`, an anchor that exists nowhere (the SPOKES editor is its own route) — fixed along with the pre-existing dead `#tasks` fallback and OperationsTab raw enum statuses, plus a reverse-direction anchor test that reds on exactly that bug class. Merged, verified in the sandbox (2056/2056), then merged into this repo as `ops/journey-hardening-2026-07` (2058/2058, eslint/tsc clean, sources byte-identical to the sandbox branch).
- **What we decided**: Britt's three product calls — orientation readiness counts ALL items everywhere; cert evidence keeps exact ID matching with a Ready-to-Work-family soft-match fallback (root cause: cert rows hardcode certType "ready-to-work" while links carry catalog ids — tracked as a follow-up, not fixed); placement alert severity stays high during the pilot.
- **Where we left off**: PR open for Britt's review — do not merge without owner sign-off. The placement-bridge Playwright spec has not run against the dev DB from this repo yet (run isolated: it toggles global `placement_bridge_classes`). Britt's uncommitted Sage prompt edits (`system-prompts.ts` open_resource/career-discovery tweak) were left untouched in the working tree and are NOT in the PR.

## Session Before That
- **Date**: 2026-07-28 (worktree `project-review-planning-96d907`, branch `claude/zealous-hellman-112f16` → PR #149)
- **What we worked on**: Root-caused and fixed the `info-certs` empty reply that #143 had only half-closed. Reproduced first (10/10 empty vs a 0/10 control), then bypassed Ollama's built-in gemma4 renderer/parser with `/api/generate` `raw: true` — which showed the literal emitted tokens to be `call:find_certification(query="…")`. Confirmed the mechanism three ways: `/v1` reports `finish_reason: "tool_calls"` plus a `reasoning` channel narrating the plan to call `lookup_program_info`, native `/api/chat` hides all of it behind `{"role":"assistant","content":""}`, and declaring the exact tool makes Ollama parse the very same output into a well-formed `tool_calls` entry. Fix is `QUALITY_EVAL_NO_TOOLS_DIRECTIVE` + `buildQualityEvalPrompt()` in the shared harness prompt module, wired into `scripts/sage-quality-eval.mjs`, plus six contract tests. Verified: 8/8 replies (was 7/8), avg FK 5.1, 0/8 over target; 666/666 Sage unit tests; ESLint clean; five of the six new checks red-baseline against pre-change text.
- **What we decided**: Harness fix only on this branch — the directive is HARNESS truth appended to the prompt, never an edit to the production guardrail. It applies to every scenario, not just grounded ones, because it is true for every scenario. Its anti-fabrication clauses were kept after a terser draft cleared the empty replies but drew "I checked our catalog for you" — invented tool use the judge would have scored as good coaching. The provider-side hole was deliberately left out: at decision time it belonged inside #147's `assertVisibleContent`, and #147 has since merged, so the follow-up now targets `main` directly.
- **Where we left off**: PR #149 open against `main`, rebased onto #147/#148 (MEMORY.md conflict resolved by keeping both entries). Note for whoever runs local evals next: the 26b model was unloaded to free the GPU (`ollama stop gemma4:26b-a4b-it-qat`) because 15 GB + 9.6 GB do not co-reside, and requests stall swapping models.

## Open Items
- [ ] **USER — day-one steps to activate PR #204** (nothing below is live until these happen, in roughly this order): (1) `COS_USER_ID`/`COS_API_TOKEN` and `TALROO_API_KEY` in `.env.local` then Render, then `npm run cos:smoke` and `npm run talroo:smoke`; (2) `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN`/`TWILIO_FROM_NUMBER` in Render, and the Twilio console's inbound webhook set to `https://visionquest.onrender.com/api/sms/inbound` (must match `APP_BASE_URL`'s scheme and host, since the signature check is over that exact URL); (3) confirm `ADMIN_DATABASE_URL` is set wherever the nudge runner and the SMS inbound webhook run — both refuse to act rather than sweep rows they cannot see if it points at `vq_app` (F63, now covering two more call sites); (4) merge #204, let Render apply migrations `20260905100000`-`20260905140000`, then run `prisma migrate deploy` on the dev DB by hand; (5) the `connect-nudges` cron job registers via `20260905140000` but is inert until #187's `app.base_url` GUC is set on prod; (6) open `connect_enabled_classes` and `sms_nudges_enabled_classes` for one pilot class (`docs/runbooks/connect-nudges.md` has the dry-run curl and the SQL verification queries); leave `connect_subsidy_lines_enabled` off until P0.8 is confirmed.
- [ ] **USER veto window on PR #204's owner-behalf decisions** (see the Key Decisions Log rows dated 2026-09-05 and the Last Session "What we decided" entry above for the full list and each one's alternative): the résumé packet contact-block strip, the retention-text/`SpokesEmploymentFollowUp` split, no backfill of existing SMS consent, `Student.studentId` as the DoHS export's student column, `interested` as a resting state with a direct-to-hired edge, the DoHS export's no-consent-scope framing, and the transaction-scoped advisory-lock rule. None of these are hard to reverse; all are named in the PR body for exactly this review.
- [ ] **F12 (extended, PR #204)**: the DoHS export column decision reuses `Student.studentId`, the same login-id-as-identifier pattern already open against the public credential page. Resolve both together once a real DoHS field list (P0.4(1)) says whether a non-authenticating report id is required instead.
- [ ] **Sage eval coverage gap (PR #204)**: no `sage:agent:eval` or `sage:redteam:eval` scenario exists yet for `propose_connection` or `search_jobs` — the tools shipped behind flags and pass the existing suite unchanged, but neither tool has been exercised by a gating eval. Needs a `GEMINI_API_KEY` available in CI to author the scenario safely (matches the standing rule that Sage-affecting PRs need their gating evals, not just unit tests).
- [ ] **P0.4(1) → `DOHS_EXPORT_COLUMNS`**: the export shipped with VisionQuest's best guess at the fields WVDE/DoHS actually wants for the FY27 review. Reconcile the column list once WVDE answers ask #1 from the memo's Part 2.
- [ ] Follow-ups noted but deliberately left out of PR #204: an e2e hydration flake (`current-target-cta` renders twice in `e2e/student-journey.spec.ts`, seen on `e81666d`, unrelated to Connect); a CI migration-drift gate comparing `CREATE TABLE`/`ENABLE ROW LEVEL SECURITY` names (this is F8's fix, not just its symptom); where "Connect" lands in the teacher nav (owner call, #11 in the design doc numbering); an Adzuna ToS email still unsent (250 calls/day, trial-then-licence terms, unrelated to this PR but blocks `DEFAULT_JOB_SOURCES` re-adding it); `Opportunity` retirement (design spec §12.5, D5) — `JobLead.sourceJobLeadId` mirrors it for now rather than replacing it; a repo-wide chore to replace `CRON_SECRET === ` comparisons with `constantTimeEqual` (every internal route does this today, PR #204 didn't change the pattern, just didn't fix it either); any e2e seed script touching both `Connection` and `Student` needs to delete `Connection` rows before `Student` rows, since the new FK ordering will make the reverse order fail.
- [ ] **USER (2026-09-04, from the NLx/MACC research)**: (D1) submit the CareerOneStop Jobs API v2 access request framed as a nonprofit SPOKES job-search integration — since 2024-08-27 new requests are reviewed by the NLx Research Hub governance board (2–3 weeks), and its exclusions name "job matching" and "platform integration"; (D2) ask Sandra Adkins whether SPOKES students must register in the MACC and whether "West Virginia Employer" is WorkForce WV's label for staff-entered postings; (D3) whether the StudentSavedJob→Application tracker gap (VQ-R-017) moves up. Memo: `docs/plans/2026-09-04-nlx-macc-job-search-research.md`. Finding: the shared handout's outcome (WV-employer postings, MACC apply = auto-recorded work-search activity for WV Works) is already deliverable by the dark `careeronestop` adapter plus its `companyName` filter; the manual six-step process itself should not be taught in-app. **Britt chose option 1 (2026-09-04) and has requested COS access; credentials pending.** Option 2 shipped dark on the same branch (`wv-employer.ts`, WV pass in the adapter, JobCard badge + MACC hint, `cos:smoke` job-board families). **Day one when the token lands:** set `COS_USER_ID`/`COS_API_TOKEN` in `.env.local`, run `npm run cos:smoke` (its `jobsearch.workforceWv*` lines answer whether NLx labels MACC rows "West Virginia Employer" and whether the apply link lands on workforcewv.org), then add both vars in Render. **Part 2 of the memo (same day, four-track research pass) reorders priorities:** the TANF evidence base says self-directed search tooling does not raise placement, brokered jobs and subsidized slots do; WV Works EIP (50% wage, 200–600 h) and ESP (100%, 6 months) subsidies are triggered only by the student's case manager; SPOKES funding is under DoHS FY27 review; the recommended build order is CareerOneStop + Talroo → employer/job-lead pipeline + exit subsidy confirm-card → SMS nudges (RCT-backed) → Sage `search_jobs`/`explain_job` → placement reporting matched to DoHS fields. Compliance flag: the Adzuna adapter may be outside Adzuna's developer terms (trial/licence, logo, 250 calls/day). AEMIS is a legacy name; the live adult-ed MIS is LACES. **2026-09-05: Britt asked for a /plan and for the missing piece — a tool that matches a student to an employer and brings them together.** Written as `docs/superpowers/specs/2026-09-05-match-and-connect-design.md` (design) + `docs/superpowers/plans/2026-09-05-match-and-connect.md` (6-phase plan, ships dark behind `connect_enabled_classes`). It is the Tier-3 job-developer channel the 2026-07-15 career-agent design deferred; its slices 1–2 (tailor_application, banded matching) already shipped. Code map facts: no `Employer` entity exists (only `Opportunity.company` / `SpokesRecord.employerName` strings), no third-party sharing consent scope, scorer never touches `Opportunity`, placement bridge off by default, per-application Verify button unwired, pathway-outcomes report script-only. Five owner decisions D1–D5 in the spec §12. **Built as PR #204 (2026-09-05, branch `claude/visionquest-job-mission-research-7pjair`, HEAD `f801ed6`)** — all six phases plus the Part 2 recommendation order (supply, then the brokering loop, then SMS nudges, then Sage search/explain, then reporting) shipped dark behind `connect_enabled_classes`/`sms_nudges_enabled_classes`. See the 2026-09-05 Last Session entry and Key Decisions Log rows for what shipped and what was decided on Britt's behalf; still awaiting her review of the design doc's five open decisions (spec §12) and the day-one activation steps above
- [ ] **USER, CRITICAL (F1 owner steps, then merge #187)**: (1) Supabase SQL Editor: `ALTER DATABASE postgres SET app.base_url = 'https://visionquest.onrender.com';` then verify via `pg_db_role_setting` (not `SHOW`, which errors on a recycled pooler session) and confirm `vault.secrets` holds `CRON_SECRET` — no `app.cron_secret` GUC exists, the review's mention was a slip; (2) decide **D4**: `ADMIN_DATABASE_URL=... npm run jobs:expire-stale -- --before=2026-06-01 --reason="..."` (dry-run, then `--apply`) or accept that `job-processor` drains the 153 rows and sends March–May mail; the three Sage jobs go live the moment the GUC is set (briefing enqueues per-student jobs at 11:00 UTC); (3) merge #187, Render deploys the migration; (4) runbook step 4: 7 rows in `cron.job`, `net._http_response` all 200, `npm run cron:health` exit 0; (5) add repo secret `CRON_CHECK_DATABASE_URL` so the nightly check runs (listed as a merge condition on #187)
- [x] **CRITICAL (F2/F3/F4) MERGED 2026-09-02 ~13:54 UTC** by Britt's instruction, squash: #188 F2 → `743537e`, #186 F3 → `6b2c888`, #185 F4 → `2a73b5d`; each verified MERGED via the GitHub API (`gh pr merge` printed nothing, as the merge-flow gotcha predicts). VQ-R-001 is closed on `main`. Worktrees and branches for the three removed. Post-merge CI on `main` at `2a73b5d` is the first run of the three together
- [ ] **D7 (from F3, Britt's call)**: six neutral-wording staff-queue alert types students previously saw are now hidden by the allowlist (`goal_stale`, `evidence_gap`, `orientation_item_incomplete`, `orientation_form_missing`, `orientation_form_revision_needed`, `requirement_noncompliant`); re-admit any by adding it to `STUDENT_VISIBLE_ALERT_TYPES` plus the pinned test
- [ ] **D8 (from F2, Britt's call)**: `syncInterventionNotifications` now delivers task/goal nudges to every active teacher (main's pre-RLS intent, previously silently blocked by WITH CHECK), which discloses student names/labels across classes in-app; narrow to assigned instructors (reuse the `findAssignedInstructors` pattern) or record program-wide as intended
- [x] **F60 (from F4)** — DONE 2026-09-03, PR #195 (`f835b10`): 988 block on 503/429/stream-error and form-lookup exits: on a provider 503 or rate-limit 429 the staff alert now fires but the student sees a generic error with no 988 block; `ensureCrisisResources` lives only on the stream success path
- [x] **F61 (from F3 audit)** — DONE 2026-09-03, PR #195: student Sage prompt filters alerts to `STUDENT_VISIBLE_ALERT_TYPES`: staff-worded alert descriptors reach the student's own Sage prompt via `bundle.alerts` → `renderRecentActivity` (`chat/send/route.ts:577-580`); one-line filter by `STUDENT_VISIBLE_ALERT_TYPES` at the call site
- [x] **F62 (from F2/F4)** — DONE 2026-09-03, PR #197 (`44fcaa1`): job/cron bodies run as the student via `withStudentRlsContext`; leftover: coordinator (D2) still has no RLS actor: `jobs-registry.ts:12` replays `handlePostResponse` with no RLS context (dormant, nothing enqueues it) — same actor-blindness class as F5; `/api/internal/alerts/sync` and the appointment-reminder cron also call `syncStudentAlerts` with no context, so the student's own nudge is dead there
- [ ] **F63 (from F2 audit)**: if `ADMIN_DATABASE_URL` is unset, `prismaAdmin` silently falls back to `DATABASE_URL` (`vq_app`) and F2 regresses quietly; add a boot probe (`SELECT rolbypassrls FROM pg_roles WHERE rolname = current_user` through `prismaAdmin`, fail fast in prod) and list the var in `render.yaml`; `wellbeing_no_recipients` (shipped in #188) is the runtime signal meanwhile. Related ops gap: `cron.job_run_details` is never purged (~195 rows/day); schedule a 7-day DELETE once `postgres` is confirmed able to delete there
- [x] **HIGH (F5, bundle 1)** — DONE 2026-09-03, PR #197: crons/jobs impersonate the student, archive/reassign audit via `tryLogAuditEvent` on the admin client, ESLint forbids app `prisma` in sessionless handlers. Residual: coordinator (D2). F7 RLS suite coverage shipped in #191: RLS-actor class — wrap per-student bodies of `internal/coaching/daily`, `alerts/sync`, `reports` in `withRlsContext` (pattern: `sage/briefing.ts:173`); replace `prisma.auditLog.create` in `teacher/students/[id]/archive` and `reassign-class` with `logAuditEvent`; grant-kpi-history; coordinator (D2). Lint rule: internal/cron/jobs code may not import the app `prisma`. Give dev a `vq_app` login so the class reproduces locally (F7)
- [x] **HIGH (F9–F11, F13–F14, F19, F59)** — DONE 2026-09-03: #193 F9 Google verified-email + MFA; #196 F10 per-account MFA/reset limits; #190 F11 ADMIN_KEY promotion role-only; #198 F13 CSV, F14 Sentry URL scrub, F19 prod rate-limit guard, F59 CSP report. **Still open: F12** (public credential page prints the login id). Original bundle text: Google OAuth skips MFA + `email_verified` + links by email; no per-account limit on MFA challenge and security-question reset (backup codes non-atomic); ADMIN_KEY promotion overwrites a teacher's password; public credential page prints the login id; Sentry scrub leaves reset-token URLs; CSV escaper swap on 2 export paths
- [x] **F66** — DONE 2026-09-03, PR #202 (`0d57137`): emailed password reset on an MFA-enabled account issues the MFA challenge cookie, not a session
- [x] **Wave 2 / #136 re-cut (F20–F29, F36, F45)** — DONE 2026-09-03: #200 F20/F21 tool results by callId + read-only cert lookup; #201 F22/F23/F25 single goal path + no XP on GET; #199 F23-UI/F24/F36/F45 goals client; #192 F26 after-write; #194 F27 appointment unique index; #195 F28/F29 interrupted reply + confirm-card rate-limit refund
- [ ] **PROD DRIFT (F8)**: `CareerAssessmentSnapshot` exists in prod with no RLS and is on no main migration (from `feature/apify-job-sources`); `JobBrowseListing` also has no RLS. Decide, then add a CI diff of CREATE TABLE vs ENABLE RLS names
- [x] Docs: `docs/audits/2026-09-01-full-review.md` and this memory — merging as PR #189
- [ ] **USER (2026-08-21)**: run the first per-role bake-off on the real host and record the host itself. (a) Record chip / unified memory / Ollama version / `OLLAMA_NUM_PARALLEL` / `OLLAMA_KEEP_ALIVE` here — **every local-model number in this repo's history has an unrecorded host, which is why they contradict each other**. (b) `npm run sage:model:bakeoff -- --models=... --json-out=reports/...` and commit the artifact (no local-model run artifact has ever been committed). (c) `sage:agent:eval` + `sage:redteam:eval --provider=ollama --model=...` on the top chat candidate — **tool selection and boundary behavior have never been measured on any local model**. (d) Set the resulting config values (SystemConfig rows, no deploy). See `docs/plans/2026-08-21-local-ai-role-models.md` §7
- [ ] **USER**: review the draft "what better means" charter (`docs/plans/2026-08-19-what-better-means-charter.md`, uncommitted in worktree `visionquest-review-improve-1ac878`; artifact link in Last Session) and make its Phase-0 calls: COS credentials, DATABASE_URL secret for the drift CI job, reading-level ceiling (6 vs 8), SAGE_AUTOPILOT_ENABLED soak, personal-Gemini-key policy, Sage-guidance oversight scope
- [ ] **USER (post charter-execution, 2026-08-20)**: the owner-gated leftovers listed in the charter's "Execution status" section — COS credentials (cos-smoke ready), SAGE_AUTOPILOT_ENABLED flip (runbook + report ready), reading-level ceiling decision (gate at 8, median 5.0), run `sage:ai:accountability` against PROD (dev fired the FERPA cloud flag), Supabase backup plan-tier verify + first restore drill, corpus triage sitting (worksheet ready), authenticated-a11y 33-violation burn-down (soak lane), OLLAMA_NUM_PARALLEL vs queue-UX call (load test says 15 students = 9 min p50)
- [x] Merge-time follow-ups vs PR #156 (`targetStudentId`) — DONE 2026-08-20 on this branch after rebasing onto the merged #156: fetchOperationsForStudent uses the OR-clause (actor rows + on-behalf-of rows), and migration `20260820140000_tighten_sage_operation_read_rls` scopes the teacher branch through `managed_student_ids()` (SageMemory precedent; applied to local + dev). Residual product call, deliberately not smuggled in: whether STUDENTS should see staff on-behalf-of ops about themselves (the student RLS clause still matches actorId only)
- [x] No-PII-in-logs sweep — **DONE 2026-08-20** (draft PR #164). All 38 sites cleared (the earlier "24" undercounted: it only looked at src/lib and missed src/app routes for login, chat, resume, applications, offboarding, and the coaching cron). Student identifiers in `logger.*` payloads are now blocked in CI by `no-restricted-syntax` in `eslint.config.mjs`; the guard was verified to actually fail (`npm run lint` exit 1 on a probe violation, 0 after revert). Correlation keys come from `studentLogKey()` (`src/lib/log-keys.ts`), provider error text from `redactContactInfo()` (`src/lib/log-redaction.ts`). **Lesson for the next sweep of this shape: search by what the identifier means, not by one literal name** — `userId` in notifications.ts hid a violation from the first `studentId`-only pass, and an ESLint selector found the rest in one shot
- [ ] **USER**: confirm retention durations in docs/DATA_RETENTION_POLICY.md (OWNER-CONFIRM markers)
- [x] `propose_resume_edit` confirm card dropped `targetStudentId` — FIXED, PR #160 merged 2026-08-20 (`6f4e62f`). **The #157 audit finding that prompted it was overstated**: it reported a dead staff-assisted flow, but that path is unreachable — the tool is `requiredRoles: ["student"]` (the executor's admin escape hatch is `role !== "student"`, so admins are refused too) and `targetStudentId` is only threaded for staff sessions. Both sides saw `undefined`, so the round-trip always worked. The fix closes a latent break, not an outage
- [ ] If `propose_resume_edit` is ever made staff-capable, `execute()` must stop using `ctx.session.id` as the resume owner — today a teacher would edit their own resume, not the student's (noted in PR #160, out of its scope)
- [ ] Product call: exempt or supply a PDF for `ai-data-consent` (release-of-information packet now ends pending-verification)
- [ ] Decide whether StudentSavedJob should carry verification fields (Application is covered)
- [ ] Career-grounding Phase B upload/sync (owner-governed per `docs/runbooks/career-grounding-sync.md`): re-stage the 15 candidates on Windows with the 2026-07-23 path/audience delta, then run the governed upload — blocked on the Mac checkout, which has neither the `docs-upload/` binaries nor `STORAGE_ACCESS_KEY`/`STORAGE_SECRET_KEY`/`STORAGE_ENDPOINT`
- [x] Ship draft PRs #138 and #142 — DONE 2026-07-27: both squash-merged (`af57cc3`, `53f9eb9`), verified via GitHub API + content checks on main
- [x] Prod seed after #142 — DONE 2026-07-27: 12 edges live in prod ProgressionEdge (read-back verified, second run proved idempotent: 0 pruned; supersedes the "still outstanding" note in Current Status above, written by a concurrent session). Caught in the act: the shipped `.mjs` seed entry couldn't link its named `.ts` import under tsx — replaced with a `.ts` entry (fix PR #145)
- [x] Dev seed — DONE 2026-07-27: dev `ProgressionEdge` seeded to the same 12 edges as prod (read-back verified row-by-row; re-run 0 pruned), so local testing exercises the table rather than the static fallback
- [x] Extend `assertVisibleContent` to tool-call finishes — SUPERSEDED, discovered by the 2026-08-21 audit: main's `b14b168` (#158 Phase 2) already classifies empty turns as tool calls on BOTH Ollama surfaces and throws `toolCallWithoutContentError`, stricter than the parked #150 (closed 2026-08-21 with evidence comment); 9-case suite in `src/lib/ai/__tests__/ollama-provider.tool-call-silence.test.ts`
- [ ] `OllamaProvider` returns an empty string for thinking models (`gemma4:26b-a4b-it-qat`): reasoning consumes all of `DEFAULT_MAX_OUTPUT_TOKENS = 768` and only `message.content` is read, so every Sage call comes back blank and `sage:quality:eval` reports 0/8. Handed to a separate session 2026-07-27; see Known Issues
- [ ] Follow-up to #142 after soak: remove the static `prerequisites` arrays from `certifications.ts` (table becomes sole source) and give `areTransitivePrerequisitesMet` its first production consumer (teacher-editable pathways)
- [ ] Memory-review rev 3 remainders: MemPalace documented-but-unwired (wire or mark deferred), CodeGraph `codegraph init` never run (run or drop from Ouroboros P-1), auto-memory durability — count `~/.claude/projects/*/memory/*.md` on the Windows machine to settle the 35-vs-19 question
- [ ] **USER**: trigger prod backfill once: `curl -X POST https://visionquest.onrender.com/api/internal/rag/backfill -H "Authorization: Bearer $CRON_SECRET"` (idempotent; needs CRON_SECRET)
- [ ] **CRITICAL-PATH (promoted 2026-07-31, was "Optional")**: provision COS_USER_ID/COS_API_TOKEN (CareerOneStop) in Render. The 2026-07-31 review found this is the single blocker for the program's core assessment→occupation→curriculum chain: the client + 5 Sage tools exist but have never hit the live API, the student RIASEC profile is LLM-inferred rather than assessed, and the interest profiler stays deferred until these credentials exist
- [x] Mobile-first redesign of the 8-step journey strip — DONE 2026-08-20, PR #162 on `claude/festive-rosalind-3b3278` (chip task_73fdd22f): summary + progress bar below `md:`, full card dashboard-only with a one-line `variant="compact"` elsewhere, step descriptions out of `title` attrs into visually-hidden prose via `stepStatusPhrase()`, "Current Target" → "Do this next". Rebased onto #163 2026-08-20 — see the composition note in Known Issues.
- [x] Consolidate the percentile implementations + modernize deprecated mock namedExports — DONE 2026-08-20 (PR #159, `d8cf43b`; chip task_48c0f870 can be dismissed). Scope was larger than the chip said: **four** implementations, not three — `sage-rag-harness.mjs` had an inline-closure fourth on the same 0–1 convention. Swap red-baselined (all four pre-change implementations replayed over a boundary fixture; outputs identical), baseline values asserted in `src/lib/percentile.test.ts` so it stays durable
- [ ] Product call: should STUDENTS see staff on-behalf-of ledger rows about themselves? (student RLS clause still matches actorId only — deliberate; also legacy staff-actor rows with NULL targetStudentId remain teacher-visible platform-wide, unavoidable without an unrecoverable backfill)
- [x] Confirm-card tokens replayable within their 10-min TTL — DONE 2026-08-21, PR #169: consume-on-execute claim in `/api/chat/tool-confirm` (`SageConfirmationUse` table, token sha256 as PK, atomic insert; P2002 = replay refused with "already used"). Claim sits after verify and before execute, and the attempt burns it. Migration `20260821100000` is **not yet applied to the shared dev DB** (remote session had no DATABASE_URL) — run `prisma migrate deploy` from a machine with dev creds
- [ ] Encode the prismaAdmin write-through bypass as a lint rule (doc block shipped in #158; "encode lessons in structure")
- [ ] Cert evidence root cause (follow-up to `ops/journey-hardening-2026-07`): cert rows are created with hardcoded `certType: "ready-to-work"` while goal links carry one of ~20 catalog ids, so exact-match evidence relies on the soft-match fallback. Fix creation to record the real catalog id + data migration; then remove `READY_TO_WORK_FAMILY_CERT_IDS` fallback in goal-evidence.ts
- [ ] Placement bridge follow-ups: per-application alert suppression (today any recorded employment date suppresses all future placement alerts — second placements/retention unhandled; see FOLLOW-UP comment in placement-bridge.ts); admin UI toggle for `placement_bridge_classes` (API landed, UI pending); rename `medianDaysResourceToActivity` → honest name when payload consumers can change
- [ ] StudentDetailTabs still lacks ARIA tablist semantics (the student-side PortfolioPage got the full pattern in the a11y slice; teacher tabs only got touch targets)
- [ ] A11y for authenticated pages: seed a test user, then extend e2e/a11y.spec.ts to authenticated routes (@axe-core/playwright + public-route suite landed 2026-07-22 with /a11y-pipeline; dark-mode scans also still open)
- [x] Dark-mode contrast sweep: StaffMfaPanel.tsx — DONE 2026-08-21: PR #130 (parked since 2026-07-22) audited as still-correct and conflict-free, refreshed against main, merged (`354f49b`) with its source regression gate
- [x] Gemini prepaid credits — RESTORED ~2026-08-21 13:27 UTC (evals green again on the very next re-run). The depletion (~12:50 UTC, every `gemini-3.1-flash-lite` call 429ing) was caused by the sweep's repeated eval runs. Residual for USER: confirm prod Sage was/is unaffected, and consider a billing alert so CI can't drain the account silently again
- [x] Merge PR #164 (log-PII sweep + lint rule) — DONE 2026-08-21 13:32 UTC, squash `0ce2c05`, after the credit-watch check-in's re-run came back fully green (sage-evals + memory + freshness + verify + e2e on `e8453e0`). Its lint rule already caught + fixed a real raw-`studentId` log in #158's `pathway-provenance.ts`. Open review question preserved from its PR body: is `studentLogKey` correlation enough for wellbeing/crisis debugging? (owner may revisit)
- [x] 2026-08-21 audit dispositions for the parked PRs — **EXECUTED 2026-08-21 afternoon on Britt's authorization**: #132/#150/#155 were already closed; #134 CLOSED (rebuild-or-close resolved as close); #154 CLOSED (machine-local precedent); #135 CLOSED + salary-parser fix salvaged as PR #175 (Apify ToS/billing, provenance reconciliation, O*NET, cluster copy remain undecided — branch `feature/apify-job-sources` restorable if the Apify call ever lands); #131 CLOSED superseded by re-cut PR #176. #136 audited in full — see the dedicated item below
- [ ] **#136 disposition (audit complete 2026-08-21, full table in PR-thread comment when Britt approves the close)**: 26/29 findings STILL LIVE on main, 1 already-on-main (004 via #173), 2 superseded-differently (023 partial — the orientation-student-profile spec still can't run in CI; 027 — charter succeeded but PRODUCT_GUIDE still shows the expired June window). Key insight: the branch's merge-conflict status is mostly an artifact of `config/remediation-ledger.json` (never landed); **17 of 27 fix commits apply near-clean to current main**, individually cherry-pickable. Recommended path: close #136 with the audit as evidence (branch `remediation/critical-high` stays the reference implementation; consider landing the ledger + `validate-remediation.mjs` on main as the tracking artifact), then re-cut in 5 themed bundles: (1) Safety+FERPA 001/002/003 — FIRST, (2) Sage-agent integrity 009/010/011/012, (3) CI instruments 021/022/026 + 023-residual, (4) goals integrity 005/007/008, (5) job board 015/016/017/018/019 (gate on re-confirming the two-tracker decision; 017's outcome half shipped via #153). **Awaiting Britt's go on the close + bundle order**
- [ ] Non-blocking follow-ups from the #175/#176 review (deliberately not smuggled into those PRs): (a) `src/lib/email-templates.ts:16` hardcodes `/settings` in the notification footer, so a teacher who gets a crisis-alert email still lands on a link the `(student)` layout bounces — `sendNotification` knows the role and could thread it through; (b) `detectPeriod` resolves by pattern-array priority, not proximity to the amount, so mixed-period prose ("$85,000 a year (about $41 per hour)") now returns `null` where main returned a right-by-accident number — fails safe, but loses a figure; same mechanism means USAJOBS `SY`/school-year rows regress to null (the honest fix is teaching `usajobs.ts` the real `RateIntervalCode` map and calling `hourlyFromAmount`); (c) `hourlyFromAmount`/`PayPeriod`/`PERIOD_HOURS` are exported with no in-tree caller — #135's Apify adapter was the consumer, so wire them to jsearch/usajobs per (b) or drop the `export`; (d) `browse-jobs.ts:31` orders `salaryMin` DESC with no `nulls: "last"`, so unknown-pay rows sort to the top (pre-existing, slightly amplified); (e) `NavBar.staff-settings.test.tsx` tests 1 and 3 assert on file existence and source text rather than the load-bearing invariant (that the `(teacher)` layout ADMITS teacher+admin) — pin the role lists against each other instead
- [ ] **Immediate one-liners from the #136 audit** (each independently shippable): CLAUDE.md:33 + AGENTS.md:32 route agents to `content/_INDEX.md` which DOES NOT EXIST; `csvEscape` in `src/lib/forms/export.ts:39-46` lacks the `=+-@` formula-injection neutralization `src/lib/csv.ts` already has (live on 2 export paths); `loop.ts:87` matches tool results by array position instead of `event.callId` (cross-wires parallel calls); `run-browse-refresh.mjs:2-5` claims a pg_cron job that doesn't exist; `academic-kpi.ts:329` labels mere-existence BHAGs as "Confirmed BHAG"
- [ ] **USER — FERPA ruling needed (VQ-R-002/003)**: `resolveAiProvider` fails OPEN to cloud when the local provider is unavailable (`src/lib/ai/provider.ts:122-128`), and resume uploads route student_record content through it. The code documents the fail-open as deliberate (provider.ts:111-117) while `.claude/rules/sage-ai.md` says student_record is local-only — same contradiction shape as the studentLogKey question. Rule which is policy, then the loser gets fixed (code or rule)
- [x] **Recommended next safety work (from the audit)** — BUILT 2026-08-21, in draft PR #173 (see Current Status): all three sources (#136 VQ-R-004, #139 `593f041`, #141) applied to main conflict-free, plus a two-reviewer hardening pass. Scope truth the audit item missed: the source PRs only ever covered the pills/overdose means family — see the follow-up item below. #173 MERGED 2026-08-21 14:56 UTC (`a7cd835`); **#139 and #141 closed as superseded with evidence comments the same hour**. #136's crisis commits are now partially superseded — its remaining scope stays under the audit-dispositions item
- [ ] Crisis follow-up — **the largest remaining coverage gap**: firearms, hanging, and jumping means patterns are absent in BOTH languages (no parked PR supplies them). Needs its own FP-tuned work: "hang out", "I'd jump at the chance", "shoot me an email", "me ahorcan los plazos" all must stay silent, and nothing red-baselines those today
- [ ] **VQ-R-001 re-cut (audited 2026-08-21: STILL LIVE, the sole critical)**: the crisis scan runs only on the model-stream success path — `detectCrisisSignal`→`recordWellbeingConcern` is reached solely via `post-response.ts:287-291` from `chat/send/route.ts:1061`; provider failure (503 at :335), stream errors (:1136), and three direct-answer returns (:218/:262/:294) all skip it, so a student's crisis message gets no alert exactly when the AI is down. Easier now than when #136 wrote it: all four unprotected exits sit in one function — hoist a never-throwing scan helper above provider resolution. Belongs in re-cut bundle 1 with the FERPA pair
- [ ] **USER**: native-speaker review of Spanish informal/euphemistic crisis register — the ES equivalent of `unalive`/`kms` filter-evasion is unassessed (the 7 EN informal-register patterns deliberately have no ES counterparts; ES colloquial forms like "me quiero morir" are covered, evasion register is not)
- [ ] Wire `platform:validate` into CI — it exits 1 on errors but NOTHING runs it (not in ci.yml, not in `pipelines:validate`); the 11-entry drift #173 fixed is the proof. Natural home: the verify job or the pipelines umbrella
- [ ] Platform-map validator extensions (from #173's review, extend-only + red-baseline per the grader rule): a full-tier char ceiling beside `COMPACT_CHAR_LIMIT` (student full grew 3,840 → 6,269 unguarded; compact sits at 649/650) and a `seeAlso` dangling-id check
- [ ] **USER veto window on #173's review-driven trades**: `end(ing) things` now ignores the "with …" breakup frame (both reviewers: highest-frequency FP for this population; recall-first exception documented in-file); `ksy` → `kys` (the typo was test-pinned, so `ksy` is now silent); "gonna die laughing"-style hyperbole still alerts (recall-first, consistent with the pinned "want to die laughing" case). Also noted: the new red-team case is a coverage canary, not a hard gate (`mustMention` is soft) — the unit tests are the actual gate
- [ ] **USER**: delete the verified-safe remote branches (this remote session's git proxy 403s deletions; 37 verified 2026-08-21 merged-PR head SHA == live SHA, plus heads of #139/#141 closed-superseded with content re-cut into main by #173; `claude/resume-previous-work-2y8gs1` deliberately excluded). Once #175/#176 are merged, `fix/salary-period-parsing` and `fix/staff-mfa-settings-reachable` join the list too. Second tier, owner-confirm (closed-unmerged, content rejected or re-cut elsewhere): `feature/sage-career-guide-scaffold` (#134), `cursor/add-cloud-dev-environment-4231` (#154), `bug/staff-mfa-settings-unreachable` (#131 → #176); keep `feature/apify-job-sources` until the Apify decision and `remediation/critical-high` until the #136 re-cuts land. One-liner: `git push origin --delete bug/spanish-crisis-parity bug/english-method-intent-parity a11y-pipeline/staffmfa-dark-contrast agent/restore-sage-stack-20260708 agent/sage-agent-readonly-20260708 agent/sage-briefing-20260708 bug/quality-eval-info-certs-grounding bug/welcome-step2-moving-target chore/audit-fix-cves chore/ci-wire-ci-pipeline-validate chore/memory-ci-gate-wrapup chore/memory-current-ci-pipeline chore/run-progression-seed chore/ship-automerge-note ci-pipeline/progression-edge ci-pipeline/teacher-nav-failed-extractions claude/busy-morse-cb72a4 claude/charming-tu-b911ff claude/dark-variant-data-theme claude/exciting-elbakyan-78e58b claude/festive-rosalind-3b3278 claude/gracious-williams-5d7277 claude/memory-system-review-fb4e1b claude/visionquest-review-improve-1ac878 claude/zealous-hellman-112f16 claude/zen-elgamal-9e172a cursor/sage-action-cards-readiness-8910 docs/career-grounding-phase-a-decisions docs/close-reasoning-budget-followup docs/memory-current-2026-08-20 docs/memory-handoff-143 docs/route-plans-corpus feat/a11y-pipeline feat/career-jobs-phase1 feat/rag-chunk-citations fix/sage-xp-staleness fix/seed-progression-entry mac-integration ops/journey-hardening-2026-07`
- [ ] Prisma 7 upgrade chore: clears the 3 remaining high advisories (deepmerge-ts via the prisma CLI) — non-force `npm audit fix` cannot (its only offer is a semver-major DOWNGRADE to 6.12.0)
- [ ] Hygiene note, action = none: migrations `20260625000000_add_job_browse_listing` and `20260625000000_add_wager_models` share a timestamp. Do NOT rename applied migrations — the ledger tracks by folder name; recorded so nobody "fixes" it
- [ ] **PROD BUG (Britt, 2026-08-21, screenshot) — "Signature submission failed." on orientation form signing.** Seen live on visionquest.onrender.com signing a 2-page SPOKES form as a student. **Triage already done, one step left that needs prod access.** The message text pins the source exactly: the client's own network-catch fallbacks append "Please try again" (`OrientationFormDetail.tsx:45`, `OrientationWizard.tsx:283`), and the screenshot has no such suffix — so this is the server's 500 catch-all at `src/app/api/forms/sign/route.ts:121` surfacing through `data.error`, i.e. the route threw an unexpected error, not a validation rejection (those return their own specific messages) and not a network failure. **Next step: read the Render logs for `"Signature submission error"` — line 120 logs `String(error)` immediately before the 500, so the actual cause is already recorded in prod.** Four candidate throw sites, most-suspect first: (1) `uploadFile()` at :71 — in prod this is an S3 `PutObjectCommand` against Supabase Storage, so bad/expired `STORAGE_ACCESS_KEY`/`STORAGE_SECRET_KEY`/`STORAGE_ENDPOINT` or a missing bucket throws here and would break EVERY signature, not just one; (2) `prisma.fileUpload.create` :74; (3) the `formSubmission.upsert` :90; (4) `syncStudentAlerts()` :112. **Note on (4) — it is a genuine design flaw regardless of what the logs say**: it runs *after* the FormSubmission is already written, still inside the try, so if it throws the student is told the submission failed when the signature actually saved — they then re-sign, and nobody knows the data is fine. Before assuming data loss for this incident, check whether the `FormSubmission` row exists in prod for that student+form. That ordering fix (move the post-write side effect out of the failure path, or wrap it) is worth doing on its own even if the root cause turns out to be storage. Severity: orientation signing gates the whole student journey, so treat as high
- [ ] **PRODUCT IDEA (Britt, 2026-08-21) — Portfolio resume should configure the Career tab.** The resume already stored in Portfolio is the richest student-authored, structured signal in the app; today the Career tab's personalization leans on LLM-inferred RIASEC + discovery chat rather than on it. Wanted: derive Career-tab settings/defaults from the resume — plausibly job-search keywords and titles, skills for `career_skills_match`, experience level, location, cluster affinity — so a student who has done the resume work does not re-enter it. **Needs research/design first**, not a build ticket yet. Open questions to answer before scoping: what the resume is actually stored as and how structured it is (vs. free text needing extraction); whether derived settings should be a one-time seed, a live re-derive, or a Sage confirm-card proposal (the repo's pattern is propose-then-human-confirm, and this is student-owned data, so a silent auto-config would break that norm); how it interacts with the existing CareerDiscovery provenance/source-label fields from #158; and whether it makes the still-unprovisioned CareerOneStop chain more or less urgent. Related: the interest profiler is deferred pending COS credentials, so resume-derived signal may be the cheapest near-term grounding available
- [ ] **PRODUCT IDEA (Britt, 2026-08-21) — Career tab job search should work more fluidly and be better.** Deliberately open-ended; needs a research pass to turn into scoped work. Start from what is already known rather than a blank page: the 2026-08-21 #136 audit found five still-live job-board defects that each degrade this exact surface — VQ-R-015 (elite big-tech boards hardcoded in the default browse pool, `adapters/ats.ts:6-32`, no seniority screen), VQ-R-016 (save button silently fails for browse jobs and unenrolled students — UI ignores the failure entirely), VQ-R-017 (two parallel application trackers, `StudentSavedJob` vs `Application`), VQ-R-018 (global `sourceId @unique` lets classes overwrite each other's listings), VQ-R-019 (three of four adapters have no fetch timeout, and one bad config aborts the whole refresh sweep). #175 just fixed salary-period parsing, which was making pay figures wrong by 1–2 orders of magnitude in the filter and match scoring. A fluidity pass should also look at the search UX itself (filters, empty states, latency, mobile at 375px) with the ux-reviewer agent, since the audit only covered correctness
- [ ] RAG corpus triage: 463 inactive ProgramDocuments need human review before embedding
- [x] Re-test the untested cell from PR #147: reasoning ON + raised output budget on `gemma4:26b-a4b-it-qat` — DONE 2026-07-27, **answer is no**. Same model, both arms back-to-back: reasoning ON @ 3072 scored **1/8 in 2223s** (7 of 8 calls hit the provider's 300s cap); reasoning OFF @ 768 scored **7/8 in 159s**. Not a contention artifact — the reasoning-off arm averaged ~20s/call on the same resident model. The shipped reasoning-off default stands; there is no globally better setting

## Key Decisions Log
Single home for engineering decisions. Product **scope** decisions live in `docs/PRODUCT_DECISIONS.md`, which stays the scope authority.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-09-02 | Student-visible `StudentAlert` types are an allowlist (`overdue_task`, `missed_appointment`) enforced in one `src/lib/student-alerts.ts` helper plus an ESLint `no-restricted-syntax` rule on student-facing globs (PR #186) | `StudentAlert` is the staff queue and RLS admits a student's own rows, so the app filter is the only guard; a denylist leaks the next staff type by default. A DB-level type restriction is blocked until the writers (`syncStudentAlerts`, `recordWellbeingConcern`) stop running in the student's own context |
| 2026-09-02 | The crisis keyword scan has exactly one call site, at the top of `POST /api/chat/send` before every exit, with an explicit atomic per-student cap (3 records / 10 min, fail-open) instead of the incidental 40/h chat limit (PR #185) | Post-response only ran after a successful stream (VQ-R-001); re-scanning in both places would race the read-then-write notification cooldowns; hoisting ahead of the limiter removed the burst ceiling, so the cap restores it without ever blocking the first signal |
| 2026-09-02 | `prismaAdmin` on the crisis/nudge path is bounded at the helper: `sendNotification*({ client: "admin" })` resolves the recipient's role and refuses non-staff, and an empty recipient set logs `wellbeing_no_recipients` (PR #188) | Under student RLS the app client cannot see teachers or insert their Notification rows, and `Promise.allSettled` hid it; an admin path bounded only by call-site provenance becomes a write-anywhere primitive for the next caller, and a CRITICAL alert with zero recipients must never be quiet (it is also the signal if `ADMIN_DATABASE_URL` is ever unset) |
| 2026-09-02 | The pg_cron re-registration migration stays unconditional; ordering (GUC → D4 → deploy) lives in the runbook, and verification reads `net._http_response`, not only `cron.job_run_details` (PR #187) | Registering jobs inactive or gating on the GUC recreates the applied-but-never-registered trap that hid this for five months; deploy-before-GUC only costs failed run rows at the same rate as healthy operation; pg_net is async, so `job_run_details` shows `succeeded` on a 401 |
| 2026-09-05 | Advisory locks in pooled code are transaction-scoped (`pg_*_xact_lock`) with distinct class ids, never session-scoped | A session-level lock is bound to the pooled backend that took it; a separate unlock call lands on a different pooled backend the moment anything else ran concurrently, leaving the lock held with nothing left to release it. Reproduced directly on the nudge runner: one leaked lock, then nine consecutive runs skipped as "already running" with no sweep happening at all. A transaction-scoped lock releases automatically when its transaction ends, on whichever backend it happened to run on, so there is no separate unlock step to land on the wrong connection |
| 2026-09-05 | A `Connection` transition detects whether its caller already holds an open transaction, rather than trusting a flag or a type list, and opens one itself only when it can (`transitionConnection` / `withTransaction` in `src/lib/connect/pipeline.ts`) | The first cut treated "a client was passed" as "the caller is already transactional", true for exactly one call site (the hire path, which wraps an `Application` write around the transition) — the other six passed a plain `prismaAdmin` and ran the status update and its `ConnectionEvent` unwrapped, so a crash between them could leave a moved connection with no ledger row, the exact data the student's `/memory` page and the instructor's audit trail are built from. Checking for a `$transaction` method is structural: no flag to set wrong, no list to keep in sync with Prisma, and a future caller passing something new gets the safe branch by default |
| 2026-09-05 | A connection cannot be withdrawn by the student once it reaches `hired` or any later status; staff retain a `closed` path throughout | `STUDENT_ALLOWED_TRANSITIONS` has no post-hire entries at all, checked directly rather than inferred, so a student cannot retract an employer disclosure after the employer has already acted on it in the real world. Staff can still close a post-hire connection (e.g. a rescinded offer), because that is a fact about the world, not a disclosure decision |
| 2026-09-05 | SMS retention replies (`Y`/`N` on the 30/60/90-day "still working" text) never write `SpokesEmploymentFollowUp` directly; they move the Connect funnel and raise a `connect_retention_confirm` staff alert for a person to record the DoHS/WIOA checkpoint | `SpokesEmploymentFollowUp` checkpoints are 1/3/6 months from `SpokesRecord.unsubsidizedEmploymentAt`; the Connect funnel counts 30/60/90 days from `Connection.startedAt` — different anchor, different scale, same unique key (`recordId, checkpointMonths`). Mapping day 30 onto month 1 automatically would risk overwriting a teacher's already-verified row with no way to tell the two apart afterward, and would strand the day-60 answer at a `checkpointMonths` value nothing reads |
| 2026-09-05 | The nudge runner allows exactly one open Y/N question per student at a time, keyed to whatever was sent in the last 72 hours, and never sends a second one while one is outstanding | Two open questions on one thread makes a bare "Y" ambiguous about which question it answers, and Twilio already overloads `YES` (opt-in keyword vs. an answer) — resolved by reading it as an answer only when a question is actually open. Serializing the state machine on one open question at a time removes the ambiguity at the source instead of trying to disambiguate replies after the fact |
| 2026-09-05 | A `STOP` reply revokes SMS for every `NotificationPreference` row sharing that phone number; every other reply (`Y`/`N`/`START`) applies to none of them when a number is shared | There is no way to tell which of two students on a shared family phone sent a given reply. Over-applying the revocation is the safe direction (nobody keeps getting texted against an implied stop); under-applying an answer would risk crediting the wrong student's job search. Documented as a known limit — those students lose the reply-based nudges until one uses a different number |
| 2026-09-05 | Date-range filters on `@db.Date` Prisma columns go through `dateOnlyBoundsUtc`; filters on real `DateTime` columns go through the existing `reportDateRangeBoundsUtc` — never the same helper for both | A `@db.Date` column has no time component in Postgres, so a `DateTime`-shaped bound silently misses or double-counts a boundary day depending on the caller's timezone assumption. Two helpers with two names make the column type an input you cannot get wrong by using the wrong wrapper reflexively |
| 2026-09-05 | Connect's teacher-facing reports (funnel, DoHS export) scope student ids through `connectClassWhere`, a class-based instructor filter, not the existing `listManagedStudentIds()` | `listManagedStudentIds()` answers "which students does this teacher manage", which is the right question for the intervention queue but the wrong one for a funnel that should show every student in a class the instructor is assigned to, matching how the rest of Connect (leads, matching, the console) already scopes by class rather than by managed-student relationship |
| 2026-09-05 | The nudge runner's per-student daily SMS cap is enforced by taking `pg_advisory_xact_lock(hashtext(studentId))` before counting and inserting the day's `OutboundMessage` row, in the same transaction | Counting-then-inserting without a lock is a check-then-act race: two nudges evaluated concurrently for the same student can both see "0 sent today" and both send, blowing the cap. The advisory lock is scoped to one transaction (`_xact_`, not the session-level lock the whole-run mutex uses) so it releases automatically even on an error, with no separate unlock step to forget |
| 2026-09-05 | The F63 boot probe (`SELECT rolbypassrls FROM pg_roles WHERE rolname = current_user` through `prismaAdmin`) is not only in the crisis/notification path anymore — the Connect nudge runner and the SMS inbound webhook both refuse to act rather than silently fall back to `vq_app` when `ADMIN_DATABASE_URL` is unset or misrouted | F63 named this as a general hazard (any `prismaAdmin` caller can regress quietly to the app role), not one specific to the original crisis path. Two more call sites now exist that would otherwise sweep or write over RLS-scoped rows they cannot actually see if the admin client silently downgrades — the fix generalizes the same probe rather than writing a second, divergent one |
| 2026-09-05 | An employer-facing packet's field list is enforced by an allowlist, and a test asserts the confirm card the student approves and the page the employer actually sees describe the identical set of fields | The design's whole consent model depends on "what the student approved" and "what got sent" never drifting apart. An allowlist checked once at send time is not enough on its own — the card-building code and the page-rendering code are two different call sites that could each individually pass the allowlist while still disagreeing with each other. The equivalence test pins both sides against one source of truth instead of two independently-correct-looking ones |
| 2026-09-05 | A long-running sweep carries a wall-clock deadline tied to its lock's transaction timeout; the lock's lifetime and the work's lifetime are never independent | Prisma's interactive-transaction timeout rolls back and releases the advisory lock but does not cancel the callback that is still running inside it, so without an explicit deadline a slow Twilio run keeps sending nudges after the lock protecting it from concurrent runs is already gone. The connect-nudges sweep now stops between sends once it is within 15 seconds of that timeout, reporting `skipped: "deadline"` for whatever it didn't reach, rather than relying on the lock to make the boundary safe |
| 2026-08-21 | Crisis detection: language parity is an invariant — a false-positive guard pinned in one language must hold in the other, and its fixtures must carry the exact tokens the pattern branch needs (PR #173) | Both reviewers independently found Spanish adherence talk ("me tomo todas las pastillas cada mañana") raising CRITICAL alerts + named instructor emails while the pinned-silent English equivalent stayed quiet — false-positive cost concentrated on the cohort least likely to have an English-fluent advocate. The ES guards had passed by omitting the quantifier, i.e. they exercised the wrong branch. Fix mirrors the EN allowlist structure (past act / intent verb / time-marker); all 15 ES must-detect rows preserved, verified individually |
| 2026-08-21 | `SAGE_PROMPT_REVISION` bumps when rendered prompt text materially grows, even when the change is a data file (platform-map, +1.1k chars student tier) rather than prompt-assembly code | The revision is an attribution tag; its failure mode is a behavior shift traced to "nothing changed". The prompt-revision.ts doc block's narrow scoping under-specifies; sage-ai.md's broad rule and the tag's purpose both say bump. Deterministic crisis regexes need no bump (#157 precedent — the 988 block is appended post-generation), so one bump covers the PR |
| 2026-08-21 | Reviews of safety-critical regex EXECUTE the patterns against adversarial corpora; fixes red-baseline every guard fixture (18/18 shown failing pre-fix) | The `ksy`/`kys` transposition was pinned BY ITS OWN TEST — no amount of diff-reading catches a typo the fixture repeats; only running "kys" against the pattern did. A fixture that passes for the wrong reason (quantifier omitted, wrong branch exercised) is worse than none: it certifies the exact hazard it was written against |
| 2026-08-21 | A Sage-affecting PR stays unmerged while its gating evals are red for INFRA reasons (depleted API credits), even when the failure is provably not the diff's (#164) | "Evals are hard gates" has no infra exception — an ungraded scenario is not a pass (the harness itself says so), and waiving the gate once creates the precedent that kills it. The cost is small (top up credits, one `rerun_failed_jobs`, merge); the alternative quietly converts a hard gate into a soft one. Distinct from the flake protocol: a flake is confirmed by a green re-run, an infra outage cannot produce one |
| 2026-08-21 | Superseded parked PRs are closed with evidence comments naming the superseding commits, never left open or closed silently (#132, #150, #155) | An open superseded PR is a standing invitation to waste a rebase on it; a silent close looks like lost work. The comment carries the proof (files, commits, what main does better), so reopening requires rebutting evidence rather than re-litigating memory |
| 2026-08-21 | Confirm tokens are single-use via an atomic insert-claim (token sha256 PK), ordered verify → claim → execute, burned by the attempt, store failure fail-closed — and deliberately NOT built on the RateLimitEntry store (PR #169) | Claim after verify so unverified requests can't write rows; claim before execute because releasing on tool error would re-open replay exactly when a partial write may have happened (a failed confirm = ask Sage for a fresh card, same bounded fail-closed philosophy as #157). Replay protection and rate limiting need opposite failure defaults — #168 is moving the rate-limit store fail-open (right for shared-classroom logins), which would silently fail replay protection open too if they shared a store |
| 2026-08-21 | Changing what a ledger column HOLDS silently re-tunes every instrument keyed on its old values — check the readers, not just the writer | `LlmCallLog.model` was changed from the provider class ("ollama") to the real model tag so per-role models would be distinguishable. `config/sage-slo.json` keys its p95 bars by exactly those old values, so the change broke the lookup in BOTH directions at once: sage_chat's cloud bar loosened 6s→45s (disabling the ~3x regression detector) and its local bar TIGHTENED 90s→45s (inventing breaches on ordinary 20-70s turns). Neither shows up as an error. The fix is not just a mapping — an unmapped model is now NAMED in the report, because a silently-substituted default is the failure mode, not a missing key |
| 2026-08-21 | An option added to only one Ollama API surface is inert on the live path, and this is a bug CLASS, not an incident | It has now happened twice: reasoning knobs (2026-07-27) and per-role keep-alive (2026-08-21, caught in review of the same session that shipped it). `postChat` tries `/v1` first and a stock Ollama answers 200, so a native-only parameter never reaches the server while looking correct in review. Guarded now by wire-level parity tests rather than by remembering. Before trusting any per-request Ollama option, curl `/v1` and confirm it is honored there |
| 2026-08-21 | AI **roles** are capability profiles keyed off the existing `AiTask`, not new per-call-site config; unset role = global model | Two call sites share a role when the same model property decides success. Hanging roles off `AiTask` gave all 11 `resolveAiProvider` call sites coverage with zero call-site edits, and a total `Record<AiTask, AiRole \| null>` makes adding a task without classifying it a type error rather than a silent fallthrough. Fail-safe direction is "behave as before": an unconfigured role must never inherit a model an operator tuned for something else |
| 2026-08-21 | The number of DISTINCT local models is the memory budget, not the number of roles | Ollama keeps every model it has served resident, and the two models already in use (15 GB, 9.6 GB) do not co-reside. Four roles on four models is four standing claims on unified memory. Three roles pointing at one model is a legitimate bake-off outcome and is the zero-config default |
| 2026-08-21 | Bake-off scoring is deterministic and its prompts are imported from production, never copied | An LLM judge needs a cloud key — which defeats the point of benchmarking the FERPA-local path — and makes runs incomparable across dates. Copying prompts into fixtures would let the benchmark drift from what the app sends. Corollary learned the hard way: fixture gold must be checked against the prompt's actual JSON skeleton, not by substring — the resume contract nests under `resume`, so asserting a bare `contact` would have failed every model and blamed the model |
| 2026-08-20 | A student id is PII in server logs, same as a name or email; `.claude/skills/student-data-privacy` and `.claude/rules/security.md` now agree (PR #164) | The skill had said "use student IDs, never names/emails" in logs, sanctioning the exact pattern the 2026-08-20 audit flagged. A studentId resolves to one student's record for anyone who can also read the DB. The two sources contradicting each other is why 38 sites accumulated: the rule file was silent on studentId, so the permissive reading won |
| 2026-08-20 | Failure paths log `studentLogKey(id)` (unsalted sha256 prefix) instead of dropping the identifier (PR #164) | Most of these paths are logged *because* nothing reached the DB (failed StudentAlert upsert, failed MoodEntry write, retry-exhausted progression update), so the log line is the only trace and an unfollowable one helps nobody. No salt: cuids are high-entropy, so there is no dictionary to run against the digest, and anyone able to hash every id to match one already has the DB. Contrast `notifications.ts`, where a Notification row exists to look up, so channel + type was enough |
| 2026-08-20 | The lint rule is the deliverable, not the 38 edits (PR #164) | An AST selector found 38 sites where a `src/lib` grep found 24, because grep never looked at `src/app`, and `userId` in notifications.ts hid from a `studentId`-only search entirely. The same rule then verified the sweep and now gates in CI. Proven to fail before being trusted: probe violation exits 1, revert exits 0 |
| 2026-08-20 | Shared `percentile()` takes p on the **0–100** scale and throws `RangeError` outside it, rather than accepting either convention (PR #159) | Three of the four callers already used 0–100 and it is what "p95" means in speech. The danger is that `percentile(xs, 0.5)` is *valid* under both conventions with wildly different meanings — the median under 0–1, essentially the minimum under 0–100 — so a silent wrong number was the failure mode, not a crash. Throwing converts any missed 0–1 call site into an immediate loud failure. Empty input returns `null` (callers wanting 0 adapt with `?? 0`) so "no data" can't be confused with a real zero |
| 2026-08-20 | A swap of a CI-adjacent instrument's internals is red-baselined by replaying the pre-change implementations over a fixture and asserting identical output (PR #159) | `sage-chat-harness` gates CI, so "the refactor looks equivalent" is not evidence. Running all four original implementations over a boundary-heavy fixture before the change and reproducing those exact outputs after proves no reported percentile moved. The captured values live in `src/lib/percentile.test.ts` rather than a scratch script, so the guarantee survives the session that established it |
| 2026-08-20 | A confirm card's `meta` must echo every field its token binds; verify an audit finding against the code before fixing it (PR #160) | `ConfirmToolCard` posts back exactly the meta fields and `/api/chat/tool-confirm` rebuilds the signed payload from them, so a bound-but-dropped field fails verification. `propose_resume_edit` hand-rolls its card instead of using `confirmationGate` and drifted. The #157 audit called this a dead staff flow; checking the code showed the path is unreachable (student-only tool, staff-only target threading), so the fix is latent-break insurance, not an outage repair — and the PR says so rather than claiming a save |
| 2026-08-20 | HMAC signature inputs are canonical JSON of the whole payload, never delimiter-joined field lists (PR #157) | A `"\|"`-join with no length prefixes leaves field boundaries ambiguous the moment any field carries free text, and `targetStudentId ?? ""` already hashed absent and empty-string identically. JSON quoting makes boundaries unambiguous and covers fields added later without anyone remembering the hazard. Corollaries pinned by tests: the payload sits under its own key so a same-named field can't be shadowed out of the HMAC by the expiry entry, and keys sort by codepoint because `localeCompare` varies with process locale/ICU |
| 2026-08-20 | Changing a token's signature input is preferred over versioning the token format when the TTL is short | #157 invalidates in-flight confirm tokens; the blast radius is one 10-minute TTL and the failure is fail-closed (verification fails, nothing executes). Carrying a format version to avoid a bounded, safe failure buys permanent complexity for a one-deploy inconvenience |
| 2026-08-20 | Journey strip collapses to "Step N of 8" + progress bar below `md:`; full card is dashboard-only, the other four student pages take a one-line `variant="compact"` (PR #162) | At 375px eight steps wrapped to ~4 rows of locked padlocks on all five pages, pushing each page's own content a viewport down. A summary line is one thing to read; an `overflow-x` snap strip was rejected because a horizontal scroll is a discovery cost for low-literacy users |
| 2026-08-20 | Step state is visually-hidden prose (`stepStatusPhrase`), never a `title` tooltip or a raw status enum | `title` is unreachable by touch and keyboard, and screen readers were being read "(locked)". A blocked step keeps saying "Needs attention." even when current — the urgent fact must not be lost to the current-step phrasing |
| 2026-08-20 | Welcome score-card auto-advance: keep the timer, 2s → 8s (owner call), exported with a floor test | Two sessions disagreed — #162 wanted it deleted (grade-5 readers lose their only save confirmation), #163 wanted it kept and made revocable (a vanishing control is not an escape hatch). Both hazards are real; 8s keeps #163's revocable mechanism and buys the reading time, so neither concern is traded away. Exported so the floor is testable rather than a number someone trims later |
| 2026-08-20 | On the #162/#163 collision, #163's auto-advance behaviour wins; #162 keeps only its theme-token and progressbar-ARIA work on ScoreCard | #163 was e2e-verified and frozen with its session closing, and `autoAdvance.start()` has one call site — removing it would hollow out `createAutoAdvance`. A peer agent cannot settle a product question, so the reading-time concern is escalated to Britt rather than resolved by whichever branch merged last |
| 2026-08-20 | `dark:` is rewired to `[data-theme]` via `@custom-variant` (PR #165) rather than converting the remaining call sites to tokens | Fixing the mechanism makes 23 already-correct lines in GoalsPageClient work as authored and removes the trap for future code. `:where()` keeps the variant at zero specificity so `dark:` still behaves like a plain utility |
| 2026-07-31 | Orientation readiness denominator counts ALL orientation items on every surface (KPI report reverted from required-only) | Seven existing call sites count all items; a per-surface split showed the same student different scores on the KPI report vs dashboard/class-progress/profile, amplified once the required welcome-video item landed |
| 2026-07-31 | Cert goal-link evidence: exact catalog-id match first, Ready-to-Work-family soft-match fallback (`READY_TO_WORK_FAMILY_CERT_IDS`) | Cert rows hardcode `certType: "ready-to-work"` while links carry catalog ids, so pure exact matching silently dropped all approved-cert evidence into the teacher review queue; borrowing evidence across unrelated certs (the original bug) stays prevented, root-cause fix tracked in Open Items |
| 2026-07-31 | Placement bridge adopted: verified accepted Application ⇒ one idempotent `placement_outcome_pending` queue item (severity high, owner call) + teacher prefill, `SpokesRecord.placementApplicationId` provenance, pilot flag `placement_bridge_classes` default-off with admin API | Closes the one structurally missing seam in the orientation→employment journey; grant KPIs and follow-ups can trace placements to source applications. High severity keeps outcome recording prompt while the pilot proves out |
| 2026-07-31 | Student-detail deep links must resolve: anchor map keys need rendered ids AND every emitted `/teacher/students/${id}#anchor` must be a mapped key (both directions tested in useAnchorTabSwitch.test.ts) | Two shipped bug instances (`#spokes-profile`, `#tasks`) navigated teachers to pages where the task could not be performed; SPOKES-editor actions link the `/spokes` route, not an anchor |
| 2026-08-20 | SageOperation attribution split: actorType/actorId = invoker, new nullable `targetStudentId` = acted-on student (create-only, no FK, indexed) | Staff on-behalf-of writes recorded only the teacher's id with no recoverable student link; no FK because ledger rows must survive student offboarding, and the upsert update clause stays status/summary-only to preserve retry idempotency |
| 2026-03-11 | Next.js + Prisma + Gemini stack | Full framework, free AI tier, conversation-first UX |
| 2026-03-11 | Named AI coach "Sage" | Wise, calm, non-judgmental mentor personality |
| 2026-03-13 | Supabase Storage over Cloudflare R2 | Single service for DB + files, simpler architecture |
| 2026-03-13 | Sentry for error tracking | Client + server + edge, free tier sufficient |
| 2026-03-13 | Standalone output mode via render.yaml | Uses `node .next/standalone/server.js` for smaller container |
| 2026-03-13 | All deps in dependencies (no devDeps) | Render sets NODE_ENV=production before build, skips devDeps |
| 2026-03-13 | Gemini 2.5-flash with model-level systemInstruction | 2.0-flash retired; chat-level systemInstruction breaks streaming |
| 2026-03-13 | Separate /teacher-register page | Clear UX separation, requires TEACHER_KEY for authorization |
| 2026-04-01 | Product docs consolidated into PRODUCT_GUIDE + PRODUCT_DECISIONS | Resolves conflicts — Vision Board, Files, Resources retained |
| 2026-04-01 | StudentDetail split into 4-tab layout | 2043→472 line parent; tabs: Overview, Goals & Plan, Progress, Operations |
| 2026-04-01 | Intervention queue as primary teacher dashboard | Urgency-scored student list above ClassOverview |
| 2026-04-01 | Goal confirmation model added | `confirmed` status, `confirmedAt`, `confirmedBy`, `lastReviewedAt` fields on Goal |
| 2026-04-01 | Unified readiness computation | Single `fetchStudentReadinessData()` used by all 6 consumers |
| 2026-04-01 | CSP headers with nonce-based scripts/styles | Hardened via `src/proxy.ts`; Gemini, Credly, Sentry, Google Fonts whitelisted |
| 2026-05-07 | Phase 2 and Phase 3 outcomes tracked as GitHub milestones | Single source of truth between `docs/PRODUCT_GUIDE.md` 90-Day Outcomes and the issue tracker. Milestones #1 (Phase 2, due 2026-05-17) and #2 (Phase 3, due 2026-06-21) with tracking issues #37–#40 carry outcome-style "definition of done" so risk-scout can flag slippage |
| 2026-05-07 | Project Autopilot installed locally | Read-only Claude Code orchestrator at `project-autopilot/` (gitignored) generates morning digests, weekly reviews, and triage sweeps over the GitHub repo. Deny list blocks all `gh` writes; agents propose, humans apply |
| 2026-06-09 | 6-phase chat-first rebuild plan locked | User-approved scope, models, UX direction |
| 2026-06-10 | Chat-first student home (Phase 4) | Sage conversation is the home surface; ambient rail carries vitals; classic dashboard kept at /dashboard/classic as a one-release parity fallback, retired 2026-07-20 — the route now redirects to /dashboard (issue #76 closed as satisfied by the redirect per PRODUCT_DECISIONS.md) |
| 2026-06-10 | Build inline, not background agents | Three background agents stalled with zero output |
| 2026-06-10 | `prisma migrate deploy` only on shared dev DB | `migrate dev` wants to reset it |
| 2026-06-10 | Semantic dedupe layers in memory extraction | Hash-only dedupe let rephrased facts through (17.4%→0.0%) |
| 2026-06-10 | Relative cosine-distance margin (0.04) for RAG | Weak shared-word FTS matches polluted top-3 under RRF |
| 2026-07-20 | Maturity repair session (see docs/MATURITY_REVIEW.md) | Verification layer added across orientation/goals/certs; crisis alerts get context cards (no transcript access — owner decision); classic dashboard deleted; crisis routing scoped to assigned instructors; data lifecycle v1; extraction dead-letter; Spanish crisis patterns |
| 2026-07-21 | Gemini safetySettings scoped to DANGEROUS_CONTENT only | Any explicit safety entry measurably shifts generation (flipped a gating tool-selection case; isolated via request-payload diff + same-hour control run on main) |
| 2026-07-21 | Eval-case demotion over gate-weakening | tool-teacher-lookup-student → non-gating tool_watch with documented restoration path, instead of widening acceptableTools |
| 2026-07-21 | PR #117 merged with a merge commit, not squash | 33 curated conventional commits worth preserving for bisection on a 6k-line change |
| 2026-07-21 | Gating tool cases vote 2-of-3 (`--samples=3`); flaky cases demote to visible `tool_watch`, never delete | Gemini tool selection non-deterministic at temp 0; invisible demotion is how canaries die (PR #118) |
| 2026-07-21 | search_forms query example de-certified ('the paper about missing class') | The example was the routing attractor three rounds of steering worked around; on the merged tree the case failed even majority voting until the attractor itself was removed |
| 2026-07-21 | Personal workflow commands (/bug, /chore, /feature) kept machine-local via .git/info/exclude | Owner call: personal ceremony definitions, not team artifacts — even the ignore rule stays out of the repo |
| 2026-07-21 | /project:fix-issue removed (PR #119) | Superseded by stricter /bug (reproducing failing test, review gate); two lanes for the same job means the looser one wins by default |
| 2026-07-22 | /ci-pipeline committed as a team command | Autonomous sibling of /feature: one plan-approval gate, then unattended build/test/CI fail-loops ending in a draft PR; never merges — matches the overnight-session working pattern |
| 2026-07-22 | Prompt artifacts gated by conformance validators (ci-pipeline:validate) | "Command matches spec" is unverifiable for a goal evaluator; a 12-check content gate makes it falsifiable, with the grader frozen during the goal run |
| 2026-07-22 | Workflow family naming: `<gate>-pipeline` (renamed /pipeline → /ci-pipeline pre-merge) | Owner plans more workflows testing other gates/lints; naming each by the gate it runs (a11y-pipeline, security-pipeline, …) keeps them differentiable |
| 2026-07-22 | Workflow roles extracted as named project agents: scout, builder, gate-runner (.claude/agents/) | Owner call: agents must be identifiable and reusable across the `<gate>-pipeline` family rather than rebuilt per workflow; gate-runner is parameterized by gate list; Plan stays with the orchestrator (owns the human gate) |
| 2026-07-22 | Issue #76 closed as satisfied-by-redirect (plan-gate Option A, PR #122) | Scout showed the ticket predated the 2026-07-20 redirect decision; PRODUCT_DECISIONS.md is scope authority; redirect stubs are the repo's retired-route pattern (/jobs, /profile); DashboardClient stays — teacher student-detail route imports it |
| 2026-07-22 | Review pass primed from tracked `.claude/agents/code-reviewer.md`; fallback dispatch labeled `code-reviewer` | Live test showed a session's agent registry may omit project agents; the pass must never be skipped or run anonymously; findings graded in the definition's CRITICAL/WARNING/SUGGESTION vocabulary, not an invented scale |
| 2026-07-22 | @axe-core/playwright as the a11y instrument (`npm run test:a11y`), public routes first | Owner call over Lighthouse/jsx-a11y: per-rule WCAG failures via the existing Playwright setup; authenticated pages wait for a seeded test user; violations fixed in the page, never by filtering rules |
| 2026-07-22 | Per-sibling pipeline validators + umbrella `pipelines:validate` as the single CI step | Sibling work must never edit a CI-enforced grader; the umbrella keeps ci.yml at one step regardless of how many `<gate>-pipeline` members exist |
| 2026-07-23 | Career-grounding Phase A locked; Phase B re-paths before upload (PR #133) | Britt's Q1–Q8 answers: audience follows document purpose, not the folder default (WIOA Referral Form + Fact Sheet move to `teachers/` / TEACHER; ECP FY25 dual-stages to `students/` + `orientation/`); interview persistence is structured profile fields as system of record plus one per-student `.md` narrative Sage appends; cert offers get a three-layer source of truth (FY-versioned catalog → classroom overlay → student profile) with `knowledge-base.ts` slimmed to defer to the catalog; interest profiler deferred until CareerOneStop/O\*NET access exists. F4 corrected — the bridge-descriptors DOCX and the RAG PDF are the same document, and the DOCX is staged because the live PDF is image-only with no extractable text |
| 2026-07-27 | Local provider disables model reasoning by default (`reasoning_effort:"none"` on `/v1`, `think:false` on `/api/chat`); opt in via `ai_provider_reasoning`, size the budget via `ai_provider_max_output_tokens` | Reasoning tokens share the output budget with the visible reply. Measured on gemma4:26b-a4b-it-qat + the real 20k-char Sage prompt: reasoning on = 0 chars of content at 768 tokens in 42.9s; reasoning off = complete reply in 21.3s at the same budget. The two Ollama surfaces take different knobs and each silently ignores the other's, so a native-only fix would have looked correct and changed nothing on the live `/v1` path. Generic OpenAI-only endpoints (LM Studio/vLLM) are deliberately left untouched — an unknown value can 400 them |
| 2026-07-27 | A truncated turn with no visible content throws; the reasoning channel is never surfaced as the reply | Returning `""` is why this shipped unnoticed — eight eval scenarios logged "empty reply" with nothing wrong in the scenarios. Surfacing `thinking` was rejected outright: it restates the system prompt's meta-instructions ("Role: Sage (Bold, supportive, practical mentor)", "Structure: Reflect before advising"), which is precisely what the eval `neverContain` canaries exist to keep out of student-facing replies |
| 2026-07-27 | `.claude/MEMORY.md` is the single home for engineering decisions, known issues, and architecture notes, and CLAUDE.md imports it | Duplicated logs in CLAUDE.md and MEMORY.md had overlapping dates, zero shared entries, and no precedence rule; the file also loaded only when an agent volunteered to read it. One home plus an `@` import makes memory load mechanically without inflating context |
| 2026-07-27 | CI-enforced graders may be extended, never relaxed — new checks must red-baseline against pre-change text (PR #137) | The frozen-grader rule guards against weakening; adding a check that provably fails on the old text is the opposite direction. A gate never observed failing is not yet a gate |
| 2026-07-27 | Adopt graph concepts in Postgres; no graph DB, no OWL/SHACL reasoner (architecture assessment artifact) | The proposed retrieve→propose→validate loop already runs here (confirm cards + instructor verification + ledger); a graph DB is a second data platform and second FERPA/RLS story; OWL is open-world and can't reject invalid writes anyway. Escalate only on measured traversal pain |
| 2026-07-27 | An eval scenario the harness cannot answer is fixed in the scenario, never by relaxing a prompt guardrail (PR #143) | `info-certs` returned empty because agent mode defers program facts to `lookup_program_info` while the quality harness generates with no tools. Weakening the tool-first rule or the anti-fabrication guardrail would have traded a real production behavior for an eval artifact; supplying the facts on the scenario — in the same `PROGRAM DOCUMENT REFERENCE` shape retrieval uses — keeps the harness faithful to production and keeps the FK reading-level score meaningful. Contract tests fail if any future scenario grades Sage on tool-gated program nouns without supplying them |
| 2026-07-27 | A tool-less eval harness must DECLARE it has no tools; grounding facts alone does not stop tool routing | #143 supplied `info-certs` its cert facts and the scenario still returned "" on 10/10 draws — the agent prompt's catalog advertises `find_certification` for exactly that question, so the model called it and Ollama's gemma4 parser stripped the call to empty content. The directive is HARNESS truth appended to the prompt, never an edit to the production guardrail, and it applies to every scenario because it is true for every scenario. Its anti-fabrication clauses are load-bearing: a terser draft cleared the empty replies but drew "I checked our catalog for you" — invented tool use the judge would have scored as good coaching |
| 2026-07-27 | An empty local-model reply is a symptom with at least three distinct causes — diagnose with raw `/api/generate` before theorizing | Three different mechanisms have now produced the same `""`: reasoning eating the output budget (#147), a tool call stripped by the model-family parser (this fix), and a stale worktree Prisma/client mismatch. `/api/generate` with `raw: true` bypasses both renderer and parser and shows the literal emitted tokens, which settled this in one call after two sessions of inference. `/v1/chat/completions` also surfaces `finish_reason` and a `reasoning` channel that native `/api/chat` hides entirely |
| 2026-07-27 | ProgressionEdge (#140/PR #142): prerequisites as data with fail-safe static fallback; TS BFS over recursive CTE; RLS Pattern L | Empty table must never unlock gated steps (Render runs migrate at deploy, seed is manual — fallback closes the window); no CTE precedent in repo, ~13 rows, visited-set BFS is unit-testable and avoids the PG-version question; static arrays stay as seed source until soak |

## Architecture Notes
- Auth: JWT in httpOnly cookies (SameSite=strict), scrypt password hashing (legacy PBKDF2 rehashed on login), TOTP MFA, `sessionVersion` invalidation
- AI providers: `src/lib/ai/` abstraction — Gemini (cloud) + Ollama (local), routed by data sensitivity (`resolveAiProvider`; student_record/staff_entered are local-only per FERPA policy); explicit safetySettings; transient-failure retry pre-first-token; prompt revisions stamped via `SAGE_PROMPT_REVISION`
- Chat: SSE streaming from `/api/chat/send` (heartbeats, disconnect handling), two-call pattern (conversation + prioritized async extraction in `src/lib/chat/post-response.ts`); token-budget-aware history trimming
- RAG: live hybrid pgvector + FTS retrieval with RRF (`src/lib/sage/hybrid-retrieval.ts`), `ProgramDocument` corpus + `catalog/` OKF layer, gating red-team/guardrail evals in CI (`.github/workflows/sage-evals.yml`)
- Safety: deterministic crisis detection (English + Spanish) with 988 safety net, structured crisis context cards to assigned instructors, failed extractions dead-lettered for teacher review
- Verification layer: orientation instructor-led steps, Sage-proposed goals, and certifications require human confirmation (intervention-queue-driven); staff reads of student data are audited
- File storage: local `./uploads/` in dev, Supabase Storage (S3-compatible) in prod
- CSRF: Origin header validation middleware for all POST/PUT/PATCH/DELETE to /api/*; Postgres RLS with spoofable-header stripping in `src/proxy.ts`
- Student routes: `(student)` route group, Teacher routes: `(teacher)` route group
- Data lifecycle: retention policy in `docs/DATA_RETENTION_POLICY.md` (durations pending OWNER-CONFIRM), admin-only offboarding with export-before-deactivate
- Sage agent: `src/lib/sage/agent/` — registry, executor (role-gated), HMAC confirm cards (`confirmation.ts`), write tools, career tools; every write ledgered in SageOperation + AuditLog
- Memory: `src/lib/sage/memory/` — RLS student-scoped, fire-and-forget extraction, weekly pg_cron consolidation
- Retrieval: hybrid pgvector+FTS RRF (`sage_hybrid_search` SQL fn); eval harnesses: `sage:rag:harness`, `sage:memory:eval`, `sage:agent:eval`
- Evals are hard gates — run all three before merging Sage-affecting changes

## Known Issues
- **No local Postgres on this Mac, and the only Supabase project is production** (2026-09-02): `.env.local` `DATABASE_URL` is the prod ref; Homebrew postgresql@16/17 are installed but stopped, Docker absent. RLS integration proof comes from the PR's CI run (hermetic pgvector container); never set `RLS_TEST_ENABLED` locally. The F1 builder validated a migration by starting an ephemeral loopback Postgres 16 with stubbed `cron`/`net`/`vault` objects and deleting it afterwards, a workable pattern for SQL that needs a real parser. Harness auto-memory holds the detail (`visionquest-only-supabase-project-is-prod`)
- **Concurrent agents in one session share one scratchpad directory** (hit 2026-09-02): two builders both wrote `scratchpad/pr-body.md`; one's `gh pr edit --body-file` briefly put the other's body on its PR. Use PR-specific filenames and re-read the live body (`gh pr view N --json body`) before any `gh pr edit`
- **Remote (claude.ai/code) sessions cannot delete remote branches** (found 2026-08-21): the session git proxy returns 403 on `git push origin --delete` while ordinary branch pushes succeed, and the GitHub MCP toolset has no delete-branch tool. Branch cleanup must run from a local checkout — the verified-safe list lives in Open Items. Related quirk from the same day: a non-fast-forward push rejection through the proxy also surfaces as `RPC failed; HTTP 403`, so a 403 on push is not necessarily auth — check whether the remote ref moved first.
- ~~**The welcome score card's 2s auto-advance is unresolved between two sessions**~~ — **Settled by Britt 2026-08-20: keep the timer, bump it to 8s.** `SCORE_ADVANCE_DELAY_MS = 8000` in WelcomeFlow.tsx, now exported and guarded by a floor test (`>= 8000`, red-baselined against the old 2000). Why it was contested: PR #162 wanted the timer gone — the card is the student's only confirmation their first quick-win saved, and at this program's grade-5 reading level two seconds did not cover one sentence plus a count. PR #163 wanted it kept but safe — revocable via `createAutoAdvance`, cancelled by every manual navigation, step-guarded, with `QuickWinsNav` always on screen and `ScoreSlot` reserving the card's space. Both were right about different hazards (#163 fixed the moving target, not reading time); 8s keeps #163's mechanism and buys the reading time. The floor is a product decision, not a tuning knob — do not trim it because a flow "feels slow".
- **Every Tailwind `dark:` utility was decoupled from the app's theme** — fixed on `claude/dark-variant-data-theme` (PR #165, stacked on #162), not yet on main. `globals.css` declares `@custom-variant dark (&:where([data-theme="dark"], [data-theme="dark"] *));`. Durable gotcha regardless: **this app themes with `[data-theme]` on <html>, never `prefers-color-scheme`**, so anything keyed to the OS media query silently disagrees with the user's in-app choice. Guarded by `src/app/globals.theme-variant.test.ts` and `e2e/theme-variant.spec.ts`.
- **Unlayered CSS in globals.css beats every Tailwind utility** (found 2026-08-20, not fixed). `@import "tailwindcss"` puts utilities in cascade layers; plain rules in globals.css are unlayered, and unlayered wins. `.surface-section`'s `border` and `background` therefore override any `border-*`/`bg-*` utility on the same element — the "My Big Vision" card on /goals asks for an amber gradient and renders slate. Check the computed style before assuming a utility applies to a `.surface-section`.
- **Concurrent logins from one IP can 500** (found 2026-08-20). The per-IP limiter has all simultaneous requests contending on the same `RateLimitEntry` row; Postgres aborts the losers and `prisma.rateLimitEntry.update()` surfaces as an unhandled 500. Not test-only — these students often sign in from one shared classroom network. Also: 10 attempts/IP/15min, so repeated local e2e runs exhaust it and every spec fails with 429. **Run local e2e at `--workers=1`.** Note `scripts/seed-e2e-users.ts` now refuses remote hosts, so it no longer clears those buckets against the dev DB.
- **`tsc --noEmit` results are cached by `tsconfig.tsbuildinfo` (`"incremental": true`), so any before/after typecheck comparison silently lies** (hit 2026-08-20). Adding then removing a type-augmentation `.d.ts` reported 0 errors both ways, which read as "the shim is dead weight"; deleting `tsconfig.tsbuildinfo` between arms showed the real 0-vs-22. Any A/B of a tsc-affecting change must `rm -f tsconfig.tsbuildinfo` before each arm. Same discipline as the local-model eviction rule below — an unreset cache inverts the conclusion
- **`@types/node` is pinned to ^20 while the runtime is Node 24, so `node:test` options can exist at runtime but not in the types.** Live instance: `mock.module()`'s `namedExports` is deprecated in Node 24 in favor of `exports`, but `MockModuleOptions` in @types/node 20 has no `exports` field, so the correct modern call fails tsc. `types/node-test-mock-module.d.ts` augments the one option (#159) and is marked for deletion once @types/node moves past 22. Expect the same shape for other post-20 `node:test` additions — augment narrowly rather than bumping the shared dependency, which several active worktrees resolve through one `node_modules`
- **`keep_alive` never reaches a stock Ollama on the live path** (verified 2026-08-21). It is a native `/api/chat` parameter; the OpenAI-compatible `/v1/chat/completions` silently drops it (ollama/ollama#11458). `postChat` tries `/v1` FIRST and real Ollama answers 200, so **`OllamaProvider.KEEP_ALIVE = "8h"` has never applied** — residency has always been governed by the host's `OLLAMA_KEEP_ALIVE` (the shipped installer sets `30m`, uniformly, for every model, alongside `OLLAMA_NUM_PARALLEL=4` and `OLLAMA_MAX_QUEUE=100`). This is the same two-surface trap as the reasoning knobs, and the per-role keep-alive shipped into it before being caught: an instance given an explicit keep-alive is now pinned to the native surface, which costs `/v1`'s `finish_reason` visibility on those calls. **Before trusting any per-request Ollama option, curl `/v1` and confirm it is honored there.**
- **`sage-load-test.mjs`'s stated premise is wrong**: its header asserts nothing in the repo sets `OLLAMA_NUM_PARALLEL`, but `scripts/install-local-ai-services.ps1:339` sets it to 4. The 15-student "9.0 min p50" figure is also a **projection** (`classroomSize x measured p50`), not a measured run, and 9.0min/15 = 36s does not match the script's own ~20-21s single-call number. Re-measure before it drives a queue-UX decision.
- ~~**MemPalace and CodeGraph are documented as available but are not wired**~~ — Superseded: both are wired on the Mac (MemPalace 2026-07-27, CodeGraph 2026-07-28) and MemPalace is backfilled (394k drawers as of 2026-08-19). Surviving gotcha: the `visionquest` room is bulk-ingest with heavy duplication (one PRODUCT_GUIDE chunk from 3 worktrees filled all top-8 hits on a product-direction query), so treat `mempalace_search` there as low-precision and prefer repo docs for product facts. Harness auto-memory holds the detail (`mempalace-codegraph-not-wired`).
- **Harness auto-memory is not backed up and does not sync between machines** — `~/.claude/projects/` is gitignored in the claude-config repo, so anything that must survive a machine reset or be visible on both machines belongs here in `.claude/MEMORY.md`, not only in auto-memory. Auto-memory holds the detail (`auto-memory-has-no-backup`).
- **A tool call the caller never offered comes back as an empty reply, not an error** (verified 2026-07-27, Ollama 0.32.4). gemma4's model template is `{{ .Prompt }}`, so Ollama uses a **built-in Go renderer/parser** for the family. When the model emits its native `call:<tool>(args)` syntax, that parser strips it out of `message.content`; if the request declared no `tools`, there is nowhere to put it and the reply is `{"role":"assistant","content":""}` on native `/api/chat` — no `tool_calls`, no error, `done_reason: "stop"`. Any prompt that advertises tools to a no-tools call can hit this. Diagnose with `/api/generate` `raw: true` (bypasses renderer AND parser, shows literal tokens); `/v1/chat/completions` is the next best surface because it exposes `finish_reason: "tool_calls"` and a `reasoning` channel that native hides. Declaring the exact tool name makes the same output parse cleanly, so this is an undeclared-tool drop, not a broken parser. `OllamaProvider.generateResponse` currently passes the `""` straight through — see Open Items for the follow-up (#147 has merged, so it now targets `main`).
- ~~**Thinking models return empty replies through `OllamaProvider`**~~ — Fixed by PR #147 (2026-07-27): reasoning is now disabled by default and a truncated turn with no visible content throws instead of returning `""`. The durable gotcha survives the fix: **Ollama's two API surfaces take different reasoning knobs and each silently ignores the other's** — `/v1/chat/completions` honors only `reasoning_effort:"none"` (and reports reasoning as `message.reasoning`), `/api/chat` honors only `think:false` (reporting `message.thinking`). `postChat` tries `/v1` first and real Ollama answers it, so the live path is OpenAI-compat; a native-only change looks correct and does nothing. Re-enable reasoning with `ai_provider_reasoning` and size it with `ai_provider_max_output_tokens`. Harness auto-memory holds the detail (`thinking-models-return-empty-via-ollama-provider`)
- **Even with its own `node_modules`, a worktree's generated Prisma client does not survive a branch switch** (hit twice on 2026-08-20). Branches now differ in their schemas, so checking out another branch leaves the client built from the previous one and `tsc` fails in files you never touched — `src/lib/sage/operations.ts` when the client predates `SageOperation.targetStudentId` (#156), `scripts/pathway-outcomes-report.ts` when it predates `Application.pathwayClusterId`. Neither error mentions the schema, which is what makes it cost time. Run `npx prisma generate` after **every** branch switch; it is safe only in a worktree with its own `node_modules` (see the next entry), and verify isolation by grepping the main checkout's `node_modules/.prisma/client/index.d.ts` afterwards
- **A worktree has no `.env.local` either** — it is gitignored and lives only in the main checkout, so from a worktree there is no `DATABASE_URL`, no dev server, and no Playwright run. Browser and e2e verification of a UI change is therefore not available from a worktree unless you deliberately copy the file in, which points the run at the shared dev DB (and several e2e specs mutate it). Say so plainly rather than implying a UI fix was seen working
- **A worktree without its own `node_modules` silently borrows the main checkout's — including its generated Prisma client** (hit 2026-07-27). Node resolution walks up to `projects/VisionQuest/node_modules`, so a worktree on `main` can be running a client generated from whatever branch the main checkout happens to sit on. Symptom: `prisma.<model>` is `undefined` and you get `Cannot read properties of undefined (reading 'upsert')` — the model exists in your schema but not in the borrowed client. **Do not "fix" this with `npx prisma generate` from the worktree**: that rewrites the shared client from your schema and strips models the other branch needs (on 2026-07-27 it would have removed `CareerAssessmentSnapshot`, which only exists on `feature/apify-job-sources`, breaking that in-flight work). Run `npm ci` inside the worktree instead, then generate — verify isolation afterwards by grepping the main checkout's `node_modules/.prisma/client/index.d.ts` for the other branch's models. The same missing-`node_modules` root cause also makes `src/lib/spokes/forms-delivery.test.ts` fail with "tsx CLI not found in node_modules"; that one is a harmless artifact, and CI passes it
- **Reasoning-off (the PR #147 default) makes `info-certs` flaky on gemma4:latest** — accepted trade, not a blocker (owner call, 2026-07-27). Four draws each, real provider + real harness prompt, models evicted between arms: reasoning ON → 555/365/614/560 chars (4/4 answered); reasoning OFF → 0/0/242/63 (2/4 empty). Mechanism: under agent mode the scenario needs the model to notice #143's `PROGRAM DOCUMENT REFERENCE` block and override the "call `lookup_program_info` first" rule, and the reasoning channel appears to be doing that judgment. Net across models: `gemma4:26b-a4b-it-qat` 0/8 → 7/8, `gemma4:latest` 8/8 → 7/8. **Resolved 2026-07-27**: reasoning ON with a raised budget does *not* beat it — on the 26b model that setting scored 1/8 in 2223s (7 of 8 calls exceeded the 300s cap) against 7/8 in 159s for the shipped default. There is no globally better setting, so treat `ai_provider_reasoning` as a **per-deployment** choice: leave it off wherever a large thinking model is configured (it is otherwise unusable), and turn it on for a smaller model like `gemma4:latest` if grounded-Q&A reliability matters more than the flakiness above
- **Any local-model A/B must evict between arms or the result inverts** — `OllamaProvider.KEEP_ALIVE = "8h"` keeps every touched model resident, so a ~25GB model starves the next one and its calls die at the provider's 300s `GENERATE_TIMEOUT_MS`, looking exactly like a code or model failure. This produced a confidently wrong conclusion on 2026-07-27 before eviction reversed it. Evict with `curl -s localhost:11434/api/chat -d '{"model":"<m>","messages":[],"keep_alive":0}'`; check with `/api/ps`. Harness auto-memory holds the detail (`ollama-keepalive-8h-starves-other-models`)
- block-no-verify commit hook false-positives when a `git commit` command shares a Bash line with any `-n` flag (grep -n, sed -n) — split commands
- Port 3000 often occupied by an unrelated local app — e2e: use BASE_URL/PORT overrides
- `gh pr checks --watch` races fresh pushes: exits "no checks reported" if launched before GitHub registers the new check run (retry after ~10s), and a trailing `echo` on the same Bash line masks the non-zero exit
- ~~docs-upload/sage-context/ is intended for RAG grounding documents~~ — Stale; that directory exists but is empty and has no bucket mapping (ingest reports it as `unmapped`). Grounding documents live as `ProgramDocument` rows in Supabase Storage, curated via the teacher documents sage-context API (`src/app/api/teacher/documents/sage-context/`) and the `catalog/` OKF (org-knowledge) layer.
- **PR #204's 13 `post-response.test.ts` failures are a known container artifact, not a regression** — the same 13-failure count against clean `main` was already established during PR #169's session (2532/2545, byte-identical failures, CI green on the same commit). Re-confirm this count hasn't drifted before assuming any new failure is one of the "known 13" rather than something PR #204 actually broke.
- **A worktree builder must `npm ci` + `prisma generate` and merge the current feature branch tip into its own branch before its findings are trusted** — same root cause as the standing "worktree borrows main's node_modules/Prisma client" gotcha, but now also applies mid-branch: a Phase N worktree cut from an earlier commit on `claude/visionquest-job-mission-research-7pjair` can be missing a model or a helper a later phase's worktree already added, and will fail in ways that look like a Phase N bug but are actually a stale base.
- **The CI RLS integration suite has two different correct shapes for "this write was blocked", and a test must match the right one** — a write blocked by a `WITH CHECK` clause throws Postgres error 42501; a write excluded by a `USING` clause on an UPDATE/DELETE instead succeeds and reports zero rows affected. Two Connect RLS tests (`917c6b3`, and its Phase 4 counterpart) exist because an earlier version of one of them asserted the wrong shape and passed for the wrong reason — it expected `count === 0` for a case that Postgres actually throws on. Write the assertion for the clause type actually in play, not by copying the shape of the nearest existing RLS test.
- **`ui-copy:readability`'s `SCAN_ROOTS` now covers the Connect surfaces** (`src/app/(student)/settings`, the advising and nudges components, `/teacher/connect`, and the new `/connect` public route) alongside the roots it already had. A grade-6 regression on any of these pages now fails the gate; it did not before PR #204 merged.
- ~~**`prismaAdmin` gets no `connection_limit`**~~ — **Fixed in PR #204** (`f801ed6`, "Second verification batch"): `withPoolDefaults()` now applies to `ADMIN_DATABASE_URL` as well as `DATABASE_URL` (`connection_limit=10&pool_timeout=10`), overridable via `ADMIN_DB_POOL_SIZE`/`ADMIN_DB_POOL_TIMEOUT`, and any pool values already present on the URL win over the defaults. Previously `applyPoolDefaults` in `src/lib/db.ts` mutated only `DATABASE_URL`, so the admin client (the crisis path, the Connect nudge runner, the SMS inbound webhook) opened connections under whatever default the driver picked rather than the pool size the app client was tuned to.
